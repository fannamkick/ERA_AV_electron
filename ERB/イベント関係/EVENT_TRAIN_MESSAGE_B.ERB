;========================================================
;調教時メッセージ
;========================================================
;eratohoRR v1.31.6より一部改変して流用
;========================================================
;調教コマンド실행중に呼び出されるテキスト
;個々のCOMFxx.ERBより呼び出し
;主に「どの調教がどのようになされたか」という情景の描写に用いる
;表示される順番はTRAIN_MESSAGE_B→TRAIN_MESSAGE_Aである事に注意
;========================================================
@TRAIN_MESSAGE_B
;調教テキスト省略設定の場合は戻る
SIF (FLAG:6 & 1)
	RETURN 0

PRINTL ‥‥‥‥‥‥‥‥‥‥‥‥‥‥‥‥‥‥‥‥‥‥‥‥‥‥‥‥‥‥‥‥‥‥‥‥‥‥‥ 

;--------------------------------------------------------
;애무
;--------------------------------------------------------
IF SELECTCOM == 0
	IF (CFLAG:40 & 64) && CFLAG:42 <= 50
		CALL PRINT_CLOTHTYPE_SPECIAL
		PRINT  너머로 
	ELSEIF CFLAG:40 & 28
		CALL PRINT_CLOTHTYPE_MAIN2
		PRINT  너머로 
	ELSEIF (CFLAG:40 & 1) || (CFLAG:40 & 2)
		PRINT 속옷 너머로 
	ENDIF
						;****슬라임パッチ追加ここから****
	IF TEQUIP:89 == 0 && TEQUIP:90 == 0 && TEQUIP:150 == 0
						;****슬라임パッチ追加ここまで****
		PRINTFORM %플레이어는()% 
		;口が汚れてないか, 主人に불결무시があるか, 助手でない時に볼개그장착중でなくキス経験済みの場合
		SIF (STAIN:0 < 2 || STAIN:0 == 16 || STAIN:0 == 17 || TALENT:MASTER:64 || ASSIPLAY) && TEQUIP:45 == 0 && CFLAG:16 != -1
			PRINT 키스하면서 
	ENDIF
	PRINTFORM %CALLNAME:TARGET%의 
	IF TALENT:135
		PRINT 미숙한 
	ELSEIF TALENT:100
		PRINT 미발달된 
	ELSEIF TALENT:115
		PRINT 포동포동하게 살찐 
	ENDIF
						;****슬라임パッチ追加ここから****
	IF TEQUIP:150
		PRINTL 몸을 슬라임이 기어다녔다…
						;****슬라임パッチ追加ここまで****
	ELSEIF TEQUIP:90
		PRINTL 몸을 촉수가 희롱했다…
	ELSEIF TEQUIP:89
		PRINTL 몸을 개의 혀가 핥아댔다…
	ELSE
		PRINTL 몸을 열심히 애무했다…
	ENDIF
	;임신중で, 出産日の5日前で즈코 인형以外の特別コスチュームか特別コスチュームでない時
	SIF TALENT:153 && CFLAG:110 <= DAY+5 && (CFLAG:42 != 11 || (CFLAG:40 & 64) == 0)
		PRINTFORML %CALLNAME:TARGET%의 둥글게 부풀어오른 배 안에서, 아이의 발길질이 희미하게 느껴진다……
;--------------------------------------------------------
;커닐링구스
;--------------------------------------------------------
ELSEIF SELECTCOM == 1
	IF TEQUIP:89
		PRINTFORM 개는 
	ELSE
		PRINTFORM %플레이어는()% 
	ENDIF
	;岩시미즈
	IF TEQUIP:57
		PRINTFORM %타겟을()% 얼굴 위에 올라타게하고, 소리를 내면서 음순을 빨아
	ELSE
		PRINTFORM %CALLNAME:TARGET%의 비순을 상냥하게 빨아 풀어
	ENDIF
	IF TALENT:121 && PALAM:5 >= PALAMLV:3
		PRINT , 단단히 발기한 페니스를 혀로 핥아
	ELSEIF TALENT:121
		PRINT , 페니스 밑을 혀로 빨아올려
	ELSEIF PALAM:5 >= PALAMLV:3
		PRINT , 부풀어오른 클리토리스를 혀로 빨아올려
	ENDIF
	PRINTL 주었다…
;--------------------------------------------------------
;애널애무
;--------------------------------------------------------
ELSEIF SELECTCOM == 2
						;****슬라임パッチ追加ここから****
	SIF TEQUIP:90 == 0 && TEQUIP:150 == 0
		PRINTFORM %플레이어는()% 
	IF TEQUIP:150
		PRINTFORML 슬라임이 %CALLNAME:TARGET%의 애널을 자극하고 있다…
						;****슬라임パッチ追加ここまで****
	ELSEIF TEQUIP:90
		PRINTFORML 점액 투성이의 촉수가 %CALLNAME:TARGET%의 애널을 자극하고 있다…
	ELSEIF TEQUIP:13
		PRINTFORML 조금씩 진동하는 애널바이브를 앞뒤로 넣고 빼기를 반복했다…
	ELSEIF TEQUIP:19
		PRINTFORML 애널비즈를 가볍게 움직여 주었다…
	ELSEIF TEQUIP:46
		PRINTFORML 배설감으로 떨리는 국혈을 플러그로 빙글빙글 돌려주었다…
	ELSEIF TEQUIP:49
		PRINTFORML 희미하게 소리가 나는 전극으로 국혈을 집요허게 괴롭혔다…
	ELSE
		PRINTFORM %CALLNAME:TARGET%의 애널을 자극
		IF PALAM:5 >= PALAMLV:3 && ABL:3 >= 3
			PRINT 하며 손 끝으로 직장까지 휘저었다…
		ELSE
		PRINTL 했다…
		ENDIF
	ENDIF
;--------------------------------------------------------
;자위
;--------------------------------------------------------
ELSEIF SELECTCOM == 3
	
	IF TALENT:122
		SAVESTR:2 = 음경을 문지르고
		SAVESTR:3 = 수컷
	ELSEIF TALENT:121 == 0
		SAVESTR:2 = 비부를 만지작거리고
		SAVESTR:3 = 암컷
	ELSEIF TALENT:121 == 1
		SAVESTR:2 = 발기한 후타나리 페니스를 문지르며 비부를 만지작거리고
		SAVESTR:3 = 암컷
	ENDIF
	
	IF A < V
		PRINTFORML %CALLNAME:TARGET%에게 자위를 명령해보았지만 거부당했다. 좀 더 지도할 필요가 있다.
	ELSEIF A > V && A < 50
		PRINTFORM %플레이어가()% 자위를 명령하자 %타겟은()% 
		;着衣設定
		IF CFLAG:40 & 8
			CALL PRINT_CLOTHTYPE_MAIN2
			PRINT 의 앞을 올려 성기를 드러내고 
		ENDIF
		SIF TALENT:15
			PRINT 자존심은 굽히지 않고 
		IF TALENT:22
			PRINT 무표정한 얼굴로 묵묵히 
		ELSEIF TALENT:10 || TALENT:14
			PRINT 떨리는 손 끝으로 
		ELSEIF TALENT:11
			PRINT 반항적으로 노려봤지만 
		ELSEIF TALENT:12
			PRINT 굴욕으로 입술을 깨물고 
		ELSEIF TALENT:35
			PRINT 수치심에 귀까지 완전히 빨개져 
		ENDIF
		SIF TEQUIP:53
			PRINT 비디오카메라 앞에서 
		PRINTL 자위를 시작했다.
	ELSEIF A >=50 && A < 80
		PRINTFORML 지도의 성과인지, %타겟은()% %CALLNAME:PLAYER%의 명령에 복종해 자위를 시작했다.
	ELSEIF A >= 80
		PRINTFORML %플레이어가()% 명령하자 기뻐하며, %타겟은()% 완전히 사육된 %SAVESTR:3%의 표정으로 자위를 시작했다.
	ENDIF
	IF TEQUIP:11 && TEQUIP:13 && ABL:2 >= 5 && ABL:3 >= 5 && ABL:0 >= 5 && ABL:1 >= 5 && PALAM:5 >= PALAMLV:4 && PREVCOM == 3
		PRINTFORML %타겟은()% 침을 흘리며, 움찔움찔 몸을 떨면서 울고 있다
		PRINTFORML 절정이 멈추지 않아 계속해서 가고 있는 것 같다…
	ELSEIF TEQUIP:11 && ABL:2 >= 5 && ABL:0 >= 5 && ABL:1 >= 5 && PALAM:5 >= PALAMLV:4 && PREVCOM == 3
		PRINTFORML %타겟은()% 애액이 흩날리는 것도 신경쓰지 않고, 바이브를 격렬하게 움직이고 있다.
		PRINTFORML 이제는 다른 일을 신경쓸 여유도 없는 것 같다…
	ELSEIF TEQUIP:13 && ABL:3 >= 5 && ABL:0 >= 5 && ABL:1 >= 5 && PALAM:5 >= PALAMLV:4 && PREVCOM == 3
		PRINTFORML %타겟은()% 부들부들 몸을 떨면서, 아누스에 바이브를 삽입하고 있다
		PRINTFORML 주름을 밀고 들어올때마다 몇번이나 절정에 도달해버리는 것 같다…
	ELSEIF ABL:10 >= 3 && ABL:0 >= 5 && ABL:1 >= 5 && PALAM:5 >= PALAMLV:4 && PREVCOM == 3
		PRINTFORML %타겟은()% 교성을 흘리며 일심분란하게 자신의 %SAVESTR:2% 있다
		PRINTFORML 멈추고 싶어도 손이 멋대로 움직여 멈춰지지 않는 것 같다…
	ENDIF

	SAVESTR:2 = %STR:0%
	SAVESTR:3 = %STR:0%

;--------------------------------------------------------
;펠라한다
;--------------------------------------------------------
ELSEIF SELECTCOM == 4
	IF TEQUIP:89
		PRINTFORM 개는 
	ELSE
		PRINTFORM %플레이어는()% 
	ENDIF
	PRINTFORM %CALLNAME:TARGET%의 
	SIF PALAM:5 >= PALAMLV:3
		PRINT 딱딱하게 발기된 
	PRINTL 페니스를 입에 삼키고 빨기 시작했다…
;--------------------------------------------------------
;가슴애무
;--------------------------------------------------------
ELSEIF SELECTCOM == 5
	IF TEQUIP:89
		PRINTFORM 개는 할짝할짝 소리를 내며 
						;****슬라임パッチ追加ここから****
	ELSEIF TEQUIP:90 == 0 && TEQUIP:150 == 0
						;****슬라임パッチ追加ここまで****
		PRINTFORM %플레이어는()% 
		SIF CFLAG:7 & 1
			PRINTL  유두를 관통하고 있는 금속제 피어스를 가볍게 잡아당기면서 
	ENDIF
	PRINTFORM %CALLNAME:TARGET%의 
	IF TALENT:116
		PRINT 완전히 평평한 
	ELSEIF TALENT:100 && TALENT:109
		PRINT 간신히 부푼 것을 알 수 있을 정도의 
	ELSEIF TALENT:109
		PRINT 표준보다 약간 작은 
	ELSEIF TALENT:110 && TEQUIP:89 == 0
		PRINT 손에서 넘쳐흐를 것 같은 
	ELSEIF TALENT:114
		PRINT 한 쪽 가슴만으로도 넘쳐흐를 것 같은 
	ENDIF
						;****슬라임パッチ追加ここから****
	IF TEQUIP:150
		PRINTFORML %CALLNAME:TARGET%의 가슴에 슬라임이 모이기 시작했다…
						;****슬라임パッチ追加ここまで****
	ELSEIF TEQUIP:90
		PRINTL 가슴을 짜내듯이, 촉수가 달라붙었다…
	ELSEIF TEQUIP:89
		PRINTL 가슴을 빨고 있다…
	ELSEIF TALENT:130 && PALAM:5 > PALAMLV:3 && TEQUIP:16 == 0 && TEQUIP:15 == 0 && TEQUIP:16 == 0 
		PRINT 유방의 끝쪽의 
		PRINTL 완전히 딱딱해진 돌기를 입으로 빨아들였다
		PRINTL 입 안에 흘러넘치듯이 대량의 모유가 흘러들어왔다…
		SIF (TALENT:85 || TALENT:90) && TEQUIP:44 == 0 && ASSIPLAY == 0 && PALAM:4 > PALAMLV:4 && TFLAG:899 == 0
			PRINTFORML 가슴에 달라붙은 %CALLNAME:PLAYER%의 머리를, %타겟은()% 사랑스럽다는듯이 꽉 껴안았다
	ELSEIF TALENT:130 && TEQUIP:16 == 0 
		PRINTL 유방에 달라붙었다
		PRINTFORML 입에 머금은 %CALLNAME:TARGET%의 유두에서, 달콤한 밀크가 분출하기 시작한다…
	ELSEIF PALAM:5 > PALAMLV:3 && TEQUIP:16 == 0 && TEQUIP:15 == 0
		PRINTL 가슴 끝의, 완전히 딱딱해진 돌기를 쥐었다…
	ELSEIF TALENT:116
		PRINTL 가슴을 더듬었다…
	ELSE
		PRINTL 가슴을 주물렀다…
	ENDIF
;--------------------------------------------------------
;키스한다
;--------------------------------------------------------
ELSEIF SELECTCOM == 6
	IF TEQUIP:89 && CFLAG:42 == 11 && (CFLAG:40 & 64)
		PRINTFORM 들개는 
		CALL PRINT_CLOTHTYPE_SPECIAL
		PRINTFORML %조사만처리(RESULT+2,"를")% 핥고 있다……
	ELSEIF CFLAG:42 == 11 && (CFLAG:40 & 64)
		PRINTFORM %플레이어는()% 
		CALL PRINT_CLOTHTYPE_SPECIAL
		PRINTFORML 에게 키스했다……
	ELSEIF TEQUIP:89
		IF TALENT:136 && TFLAG:899 == 0
			PRINTFORML %타겟과()% 들개는 마치 사이 좋은 연인처럼 
			PRINTFORML 할짝할짝 서로의 혀를 빨았다……
			SIF TALENT:85 && TFLAG:899 == 0
				PRINTFORML 그 모습은 옆에서 보고 있으면 질투를 느낄 정도다……
		ELSEIF ABL:39 >= 3 && TFLAG:899 == 0
			PRINTFORML 개가 짐승의 숨결을 내뿜으며, %CALLNAME:TARGET%의 구강을 구석구석까지 빨면, 
			PRINTFORML %CALLNAME:TARGET%도 혀를 뻗어 개의 입 안을 빨아주었다……
		ELSE
			PRINTFORML 개는 짐승의 숨결을 토하며, %CALLNAME:TARGET%의 구강을 구석구석까지 빨았다…
		ENDIF
	ELSE
		PRINTFORM %플레이어는()% %CALLNAME:TARGET%의 구강을 구석구석까지 빨
		IF ABL:10 >= 2 && EXP:40 >= 1000 && TALENT:PLAYER:122 == 0 && TFLAG:899 == 0
			PRINTFORML 며, 끈적하게 혀를 섞어 타액을 교환했다…
		ELSEIF ABL:10 >= 2 && TFLAG:899 == 0
			PRINTFORML 았다, 그러자 %CALLNAME:TARGET%도 적극적으로 혀를 섞어왔다…
		ELSE
			PRINTL 았다…
		ENDIF
		SIF CFLAG:7 & 16
			PRINTFORML %CALLNAME:TARGET%의 혀 끝에 피어스의 감촉이 느껴진다……
	ENDIF
;--------------------------------------------------------
;조개벌리기
;--------------------------------------------------------
ELSEIF SELECTCOM == 7

	IF FLAG:36 == 0
		SAVESTR:2 = %STR:0%
	ELSEIF CFLAG:6 == 0 - 2
		SAVESTR:2 = 영구 탈모된
	ELSEIF CFLAG:6 == 0 - 1 || CFLAG:6 == 0 || (CFLAG:6 <= 5 && TALENT:125)
		SAVESTR:2 = 털 없는
	ELSEIF CFLAG:6 <= 5
		SAVESTR:2 = 매끈매끈하게 깎은
	ELSEIF CFLAG:6 <= 7
		SAVESTR:2 = 자라기 시작한 음모로 덮인
	ELSEIF CFLAG:6 <= 9
		SAVESTR:2 = 엷은 음모로 덮인
	ELSEIF CFLAG:6 <= 12
		SAVESTR:2 = 음모로 덮인
	ELSEIF CFLAG:6 <= 18
		SAVESTR:2 = 마음껏 자란 음모로 우거진
	ELSEIF CFLAG:6 > 18
		SAVESTR:2 = 애널까지 무성한 음모로 덮인
	ENDIF

	PRINTFORM %CALLNAME:PLAYER%에게 명령받아, %타겟은()% 
	SIF TEQUIP:53
		PRINT 비디오카메라 앞에서 
	;着衣設定
	IF CFLAG:40 & 8 && ABL:17 != 0
		CALL PRINT_CLOTHTYPE_MAIN2
		PRINT 의 앞을 크게 젖히고, 
	ENDIF
	;노출벽
	IF ABL:17 == 0
		IF TALENT:22
			PRINT 무표정한 얼굴로 
		ELSEIF TALENT:10 || TALENT:14
			PRINT 떨면서 
		ELSEIF TALENT:11
			PRINT 반항적으로 노려봤지만, 재차 명령하니 마지못해하는 모습으로 
		ELSEIF TALENT:12
			PRINT 굴욕으로 입술을 깨물며 
		ELSEIF TALENT:35
			PRINT 부끄러움에 귀까지 새빨갛게 되어서 
		ENDIF
		;着衣設定
		IF CFLAG:40 & 8
			CALL PRINT_CLOTHTYPE_MAIN2
			PRINT 의 옷자락을 크게 걷어올리고, 
		ENDIF
		PRINTFORML 자신의 %SAVESTR:2% 음렬에 손가락을 넣어, 살며시 좌우로 벌려보였다
	ELSEIF ABL:17 == 1
		PRINTFORML 얼굴을 새빨갛게 물들이면서도, 흠칫흠칫, %SAVESTR:2% 성기를 분명하게 벌려보였다
	ELSEIF ABL:17 == 2
		PRINTFORML 하복부에 손을 대고, 집게와 중지로 싸인을 그리듯이, %CALLNAME:PLAYER%에게 %SAVESTR:2% 자신의 비소를 노출했다
	ELSEIF ABL:17 == 3
		PRINTFORM 기쁨에 찬 표정으로 한 손으로 %SAVESTR:2% 음순을 벌리고, 빈 손으로 
		IF TALENT:121
			PRINT 페니스
		ELSE
			PRINT 음핵
		ENDIF
		PRINTL 을 만지작거리기 시작했다
	ELSEIF ABL:17 == 4
		PRINTFORML 양손의 손가락을 동시에 사용해 %SAVESTR:2% 음순뿐만이 아니라, 질구도 동시에 벌려보였다
	ELSEIF ABL:17 >= 5
		PRINTFORML 양손의 손가락을 %SAVESTR:2% 음순에 대고, 질구까지 %CALLNAME:PLAYER%에게 보이도록 크게 벌렸다
	ENDIF
	IF TFLAG:38 >= 2
		PRINTFORML 벌려진 질구에서 질내를 채우고 있던 정액이 계속해서 넘쳐흐른다……
	ELSEIF TFLAG:38
		PRINTFORML 열린 음순에서 사정한지 얼마 안되는 정액이 흐르기 시작했다……
	ENDIF

	;정조대
	IF CFLAG:42 == 79 && (CFLAG:40 & 64) 
		PRINTFORML 그리고 %CALLNAME:TARGET%의 바기너는 튼튼한 정조대로 확실히 지켜지고 있다
	ELSE
		PRINTFORM %CALLNAME:TARGET%의 
			IF PALAM:3 >= 3000
				PRINTFORM 애액에 젖어서 요염하게 빛나는 
			ENDIF

			IF CFLAG:608 == 0 && TALENT:0 == 1
				PRINTFORML 바기나는 한 번도 사용된적 없는 핑크색이다
			ELSEIF CFLAG:608 >= 1 && TALENT:0 == 1
				PRINTFORM 거듭된 성행위로 비대해진 라비아는 
				IF CFLAG:608 == 2
					PRINTFORML 검게 변색되었다
				ELSEIF CFLAG:608 == 1
					PRINTFORML 예쁜 핑크색이다
				ENDIF
			ELSEIF CFLAG:608 == 0 && TALENT:0 == 0
				PRINTFORML 바기나는 신품처럼 예쁜 핑크색이다
				PRINTFORM %플레이어가()% 첫체험에 대해 묻자 %타겟은()% 
					IF TALENT:85 == 0 || TALENT:76 == 0
						PRINTFORM 부끄러워하면서도 
					ENDIF
				PRINTFORM 
					IF CFLAG:15 == 16
						PRINTFORML %플레이어가()% 첫 상대라고 고백했다…
							IF TALENT:85 == 1
								PRINTFORM %타겟은()% %CALLNAME:MASTER%에게 처녀를 
								PRINTFORML 바친 것을 행복해하고 있다
							ENDIF
					ELSEIF CFLAG:15 == 2
						PRINTFORM %CALLNAME:MASTER%의 지시로 매춘을 한 이름 모를 남자손님에게 
						PRINTFORML 처녀를 빼앗긴 것을 %CALLNAME:PLAYER%에게 고백했다…
					ELSEIF CFLAG:15 == 3
						PRINTFORM %CALLNAME:MASTER%의 지시로 매춘을 한 이름 모를 여자손님에게 
						PRINTFORML 처녀를 빼앗긴 것을 %CALLNAME:PLAYER%에게 고백했다…
					ELSEIF CFLAG:15 == 4
						PRINTFORM 바이브로 처녀를 빼앗긴 것과 그 일에 대한 것을 
						PRINTFORML %CALLNAME:PLAYER%에게 고백했다…
					ELSEIF CFLAG:15 == 5
						PRINTFORM 극태 바이브로 처녀를 빼앗긴 것과 그 일에 대한 것을 
						PRINTFORML %CALLNAME:PLAYER%에게 고백했다…
					ELSEIF CFLAG:15 == 6
						PRINTFORM %CALLNAME:MASTER%의 지시로 매춘을 한 이름모를 손님에게 
						PRINTFORML 처녀를 빼앗긴 것을 %CALLNAME:PLAYER%에게 고백했다…
					ELSEIF CFLAG:15 == 7
						PRINTFORM 치한에게 억지로 범해져 처녀를 빼앗긴 것을 
						PRINTFORML %CALLNAME:PLAYER%에게 고백했다…
					ELSEIF CFLAG:15 == 8
						PRINTFORML %CSTR:0%의 이름과 그 경위를 %CALLNAME:PLAYER%에게 고백했다…
							IF TALENT:85 == 1
								PRINTFORM %CALLNAME:TARGET%은 %CALLNAME:MASTER%에게 처녀를
								PRINTFORML 바치지 못한 것을 죄송스러워 했다
							ENDIF
					ELSEIF CFLAG:15 == 11
						PRINTFORM 불량배에게 억지로 범해져 처녀를 빼앗긴 것을 
						PRINTFORML %CALLNAME:PLAYER%에게 고백했다…
					ELSEIF CFLAG:15 == 14
						PRINTFORM 촉수에게 처녀를 빼앗긴 것을 
						PRINTFORML %CALLNAME:PLAYER%에게 고백했다…
					ELSE
						PRINTFORML %CSTR:0%의 이름과 첫체험에 대해 %CALLNAME:PLAYER%에게 고백했다…
							IF TALENT:85 == 1
								PRINTFORM %타겟은()% %CALLNAME:MASTER%에게 처녀를 
								PRINTFORML 바치지 못한 것을 죄송스러워 했다
							ENDIF						
					ENDIF
			ELSEIF CFLAG:608 >= 1 && EXP:0 > 50 && TALENT:0 == 0
				PRINTFORM 거듭된 성행위로 비대해진 라비아는 
				IF CFLAG:608 == 2
					PRINTFORM 검게 변색되었으
				ELSEIF CFLAG:608 == 1
					PRINTFORM 예쁜 핑크색이
				ENDIF
				PRINTFORML 며 몇번이고 페니스에 꿰뚫린 질구도 크게 벌려져 있다
				PRINTFORM %플레이어가()% 첫체험에 대해서 묻자 %타겟은()%
					IF TALENT:85 == 0 || TALENT:76 == 0
						PRINTFORM 부끄러워하면서도 
					ENDIF
				PRINTFORM 
					IF CFLAG:15 == 16
						PRINTFORML %플레이어가()% 첫 상대라고 고백했다…
							IF TALENT:85 == 1
								PRINTFORM %타겟은()% %CALLNAME:MASTER%에게 처녀를 
								PRINTFORML 바친 것을 행복해하고 있다
							ENDIF
					ELSEIF CFLAG:15 == 2
						PRINTFORM %CALLNAME:MASTER%의 지시로 매춘을 한 이름 모를 남자손님에게 
						PRINTFORML 처녀를 빼앗긴 것을 %CALLNAME:PLAYER%에게 고백했다…
					ELSEIF CFLAG:15 == 3
						PRINTFORM %CALLNAME:MASTER%의 지시로 매춘을 한 이름 모를 여자손님에게 
						PRINTFORML 처녀를 빼앗긴 것을 %CALLNAME:PLAYER%에게 고백했다…
					ELSEIF CFLAG:15 == 4
						PRINTFORM 바이브로 처녀를 빼앗긴 것과 그 일에 대한 것을 
						PRINTFORML %CALLNAME:PLAYER%에게 고백했다…
					ELSEIF CFLAG:15 == 5
						PRINTFORM 극태 바이브로 처녀를 빼앗긴 것과 그 일에 대한 것을 
						PRINTFORML %CALLNAME:PLAYER%에게 고백했다…
					ELSEIF CFLAG:15 == 6
						PRINTFORM %CALLNAME:MASTER%의 지시로 매춘을 한 이름 모를 여자손님에게 
						PRINTFORML 처녀를 빼앗긴 것을 %CALLNAME:PLAYER%에게 고백했다…
					ELSEIF CFLAG:15 == 7
						PRINTFORM 치한에게 억지로 범해져 처녀를 빼앗긴 것을 
						PRINTFORML %CALLNAME:PLAYER%에게 고백했다…
					ELSEIF CFLAG:15 == 8
						PRINTFORML %CSTR:0%의 이름과 그 경위를 %CALLNAME:PLAYER%에게 고백했다…
							IF TALENT:85 == 1
								PRINTFORM %CALLNAME:TARGET%는 %CALLNAME:MASTER%에게 처녀를
								PRINTFORML 바치지 못한 것을 죄송스러워 했다
							ENDIF
					ELSEIF CFLAG:15 == 11
						PRINTFORM 불량배에게 억지로 범해져 처녀를 빼앗긴 것을 
						PRINTFORML %CALLNAME:PLAYER%에게 고백했다…
					ELSEIF CFLAG:15 == 14
						PRINTFORM 촉수에게 처녀를 빼앗긴 것을 
						PRINTFORML %CALLNAME:PLAYER%에게 고백했다…
					ELSE
						PRINTFORML %CSTR:0%의 이름과 그 일에 대한 것을 %CALLNAME:PLAYER%에게 고백했다…
							IF TALENT:85 == 1
								PRINTFORM %타겟은()% %CALLNAME:MASTER%에게 처녀를 
								PRINTFORML 바치지 못한 것을 죄송스러워 했다
							ENDIF						
					ENDIF
			ELSEIF CFLAG:608 >= 1 && EXP:0 <= 50 && TALENT:0 == 0
				PRINTFORM 거듭된 성행위로 비대해진 라비아는 
				IF CFLAG:608 == 2
					PRINTFORM 검게 변색되었으
				ELSEIF CFLAG:608 == 1
					PRINTFORM 예쁜 핑크색이
				ENDIF
				PRINTFORML いる
				PRINTFORM %플레이어가()% 첫체험에 대해서 묻자 %타겟은()%
					IF TALENT:85 == 0 || TALENT:76 == 0
						PRINTFORM 부끄러워하면서도 
					ENDIF
				PRINTFORM 
					IF CFLAG:15 == 16
						PRINTFORML %플레이어가()% 첫 상대라고 고백했다…
							IF TALENT:85 == 1
								PRINTFORM %타겟은()% %CALLNAME:MASTER%에게 처녀를 
								PRINTFORML 바친 것을 행복해하고 있다
							ENDIF
					ELSEIF CFLAG:15 == 2
						PRINTFORM %CALLNAME:MASTER%의 지시로 매춘을 한 이름 모를 남자손님에게 
						PRINTFORML 처녀를 빼앗긴 것을 %CALLNAME:PLAYER%에게 고백했다…
					ELSEIF CFLAG:15 == 3
						PRINTFORM %CALLNAME:MASTER%의 지시로 매춘을 한 이름 모를 여자손님에게 
						PRINTFORML 처녀를 빼앗긴 것을 %CALLNAME:PLAYER%에게 고백했다…
					ELSEIF CFLAG:15 == 4
						PRINTFORM 바이브로 처녀를 빼앗긴 것과 그 일에 대한 것을 
						PRINTFORML %CALLNAME:PLAYER%에게 고백했다…
					ELSEIF CFLAG:15 == 5
						PRINTFORM 극태 바이브로 처녀를 빼앗긴 것과 그 일에 대한 것을 
						PRINTFORML %CALLNAME:PLAYER%에게 고백했다…
					ELSEIF CFLAG:15 == 6
						PRINTFORM %CALLNAME:MASTER%의 지시로 매춘을 한 이름 모를 여자손님에게 
						PRINTFORML 처녀를 빼앗긴 것을 %CALLNAME:PLAYER%에게 고백했다…
					ELSEIF CFLAG:15 == 7
						PRINTFORM 치한에게 억지로 범해져 처녀를 빼앗긴 것을 
						PRINTFORML %CALLNAME:PLAYER%에게 고백했다…
					ELSEIF CFLAG:15 == 8
						PRINTFORML %CSTR:0%의 이름과 그 경위를 %CALLNAME:PLAYER%에게 고백했다…
							IF TALENT:85 == 1
								PRINTFORM %CALLNAME:TARGET%는 %CALLNAME:MASTER%에게 처녀를
								PRINTFORML 바치지 못한 것을 죄송스러워 했다
							ENDIF
					ELSEIF CFLAG:15 == 11
						PRINTFORM 불량배에게 억지로 범해져 처녀를 빼앗긴 것을 
						PRINTFORML %CALLNAME:PLAYER%에게 고백했다…
					ELSEIF CFLAG:15 == 14
						PRINTFORM 촉수에게 처녀를 빼앗긴 것을 
						PRINTFORML %CALLNAME:PLAYER%에게 고백했다…
					ELSE
						PRINTFORML %CSTR:0%의 이름과 그 일에 대한 것을 %CALLNAME:PLAYER%에게 고백했다…
							IF TALENT:85 == 1
								PRINTFORM %타겟은()% %CALLNAME:MASTER%에게 처녀를 
								PRINTFORML 바치지 못한 것을 죄송스러워 했다
							ENDIF						
					ENDIF
			ENDIF
	ENDIF
	
	;피어싱状態
	SIF CFLAG:7 & 8 || CFLAG:7 & 4
		PRINTFORM %CALLNAME:TARGET%의 
	IF CFLAG:7 & 8
		SIF PALAM:5 >= PALAMLV:4
			PRINT 발기한 
		IF TALENT:121
			PRINT 페니스
		ELSE
			PRINT 클리토리스
		ENDIF
		SIF CFLAG:7 & 4
			PRINT 와 
	ENDIF
	SIF CFLAG:7 & 4
		PRINT 좌우의 음순
	SIF CFLAG:7 & 8 || CFLAG:7 & 4
		PRINTL 에서는, 부드러운 살을 뚫고 금속제의 피어스가 빛나고 있다……

	SAVESTR:2 = %STR:0%

;--------------------------------------------------------
;손가락삽입
;--------------------------------------------------------
ELSEIF SELECTCOM == 8
	PRINTFORM %플레이어는()%, %CALLNAME:TARGET%의 
	;着衣設定
	IF CFLAG:40 & 8 && ABL:17 != 0
		CALL PRINT_CLOTHTYPE_MAIN2
		PRINT 의 옷자락을 걷어올리고, 
	ENDIF
	IF EXP:0 == 0
		PRINT 남자를 모르는 
	ELSEIF PALAM:3 >= PALAMLV:4
		PRINT 젖어있는 
	ENDIF
	IF TALENT:132
		PRINT 어린 균열
	ELSE
		PRINT 비렬
	ENDIF
	IF TEQUIP:11
		PRINTFORML  안에서, 요염하게 꾸물거리는 바이브를 앞뒤로 움직였다
	ELSE
		PRINTFORM 에 자신의 손가락을 
		IF PREVCOM == 8 && ABL:PLAYER:12 >= 3
			PRINTFORML 삽입하고 상냥하게 움직이고 있다…
		ELSE
			PRINTFORML 천천히 삽입했다…
		ENDIF
	ENDIF
	;G스팟자극
	IF TEQUIP:11 && PREVCOM == 8 && ABL:PLAYER:12 >= 3
		PRINTFORML %플레이어는()% 교묘하게 바이브를 조종해, 민감한 천장을 꾹 눌렀다…
	ELSEIF PREVCOM == 8 && ABL:PLAYER:12 >= 3
		PRINTFORML %플레이어는()% %CALLNAME:TARGET%의 질 안에서 손가락을 굽혀, 부풀어있는 천장을 자극했다…
	ELSEIF TEQUIP:11 && PALAM:5 >= PALAMLV:3 && ABL:2 >= 2
		PRINTFORML %CALLNAME:TARGET%의 질은, 꿀에 젖어 바이브를 끈적끈적 휘감고 있다…
	ELSEIF TEQUIP:11 && PALAM:5 <= PALAMLV:3 && ABL:2 >= 2
		PRINTFORML %타겟은()% 바이브의 자극에 괴로워하고 있다…
	ELSEIF TEQUIP:11 && PALAM:5 >= PALAMLV:3 && ABL:2 <= 3
		PRINTFORML %타겟은()% 뺨을 붉히고 때때로 소리를 높이고 있다…
	ELSEIF TEQUIP:11 && PALAM:5 <= PALAMLV:3 && ABL:2 <= 3
		PRINTFORML %타겟은()% 조금 괴로운듯한 표정을 하고 있다…
	ELSEIF EXP:0 <= 1
		PRINTFORML %타겟은()% 질내의 이물감을 무서워하고 있다…
	ELSEIF EXP:0 >= 1 && EXP:0 <= 30
		PRINTFORML %타겟은()% 손가락의 감촉을 한숨을 내쉬며 참고 있다…
	ELSEIF EXP:0 >= 31 && EXP:0 <= 50
		PRINTFORML %타겟은()% 손가락의 감촉을 즐기고 있는 것 같다…
	ELSEIF EXP:0 >= 51 && EXP:0 <= 80
		PRINTFORML %타겟은()% 손가락이 움직이는 감촉에 넋을 잃고 있다…
	ELSEIF EXP:0 >= 81 && EXP:0 <= 120
		PRINTFORML %타겟은()% 배안을 휘저어지는 감각에 뜨거운 숨길을 토해내고 있다…
	ELSEIF EXP:0 >= 121
		PRINTFORML %타겟은()% 침을 늘어뜨리고, 스스로 허리를 흔들며 쾌락을 탐내고 있다…
	ENDIF
;--------------------------------------------------------
;애널핥기
;--------------------------------------------------------
ELSEIF SELECTCOM == 9
	IF TEQUIP:89
		PRINTFORM 개는 뜨거운 숨을 내뿜으며, 
	ELSE
		PRINTFORM %플레이어는()% 
	ENDIF
	PRINTFORM %CALLNAME:TARGET%의 
	SIF EXP:1 <= 10
		PRINT 단단히 닫혀있는 
	PRINT 아누스를 상냥하게 핥아 풀어주
	SIF EXP:1 >= 50 && PALAM:5 >= PALAMLV:3
		PRINT 고, 혀 끝을 넣어 구멍 안까지 휘저
	PRINTL 었다…
;--------------------------------------------------------
;로터
;--------------------------------------------------------
ELSEIF SELECTCOM == 10
	PRINTFORM %플레이어는()% %CALLNAME:TARGET%의 
	IF TALENT:122 && PALAM:5 >= PALAMLV:3
		PRINTL 발기한 페니스에 진동하는 로터를 꽉 눌렀다…
	ELSEIF TALENT:122
		PRINTL 페니스에 진동하는 로터를 꽉 눌렀다…
	ELSEIF TALENT:121 && PALAM:5 >= PALAMLV:3
		PRINTL 질퍽거리는 음순에 진동하는 로터를 꽉 눌렀다…
	ELSEIF TALENT:121
		PRINTL 음순에 진동하는 로터를 꽉 눌렀다…
	ELSEIF PALAM:5 >= PALAMLV:3
		PRINTL 단단하게 솟아오른 클리토리스에 진동하는 로터를 꽉 눌렀다…
	ELSE
		PRINTL 클리토리스에 진동하는 로터를 꽉 눌렀다…
	ENDIF
;--------------------------------------------------------
;바이브
;--------------------------------------------------------
ELSEIF SELECTCOM == 11
	IF TEQUIP:11
		PRINTFORM %CALLNAME:TARGET%에게서 꿈틀거리는 
		SIF TALENT:76 == 1
			PRINTFORM 극태 
		PRINTFORML 바이브를 천천히 뽑아냈다
		;失神してない
		IF TFLAG:899 == 0
		;△なんでTALENT:85(연심)?
			IF EXP:0 >= 201 && TALENT:85
				PRINTFORM %타겟은()% 질내의 상실감을 견디기 힘든듯 
				SIF TALENT:45 == 0 && TALENT:310 == 0
					PRINTFORM 눈물과 
				PRINTFORM 침을 흘리 
			ELSEIF EXP:0 >= 151 && EXP:0 <= 200 && TALENT:85
				PRINTFORM %CALLNAME:TARGET%의 질내에 모여있던 점액이 긁어내져 방울방울 떨어져, 웅덩이를 만들
			ELSEIF EXP:0 >= 121 && EXP:0 <= 150 && TALENT:85
				PRINTFORM 바이브가 뽑힌 %CALLNAME:TARGET%의 질벽에서는 뚝뚝하고 애액이 방울져 떨어지
			ELSEIF EXP:0 >= 71 && EXP:0 <= 120 && TALENT:85
				PRINTFORM %타겟은()% 우물쭈물하며 사타구니를 문지르며 %조사처리(CALLNAME:PLAYER,"를")% 보
			ELSEIF EXP:0 >= 41 && EXP:0 <= 70 && TALENT:85
				PRINTFORM %타겟은()% 뜨거운 한숨을 토해내
			ELSEIF EXP:0 >= 21
				PRINTFORM %타겟은()% 느슨해진 질구를 벌리
			ELSEIF EXP:0 >= 1 && EXP:0 <= 20
				PRINTFORM %타겟은()% 안심한듯한 표정을 하
			ENDIF
			PRINTL 고 있다…
		ENDIF
	ELSE
		PRINTFORM %CALLNAME:TARGET%의 
		SIF PALAM:3 >= PALAMLV:5
			PRINT 충분히 
		SIF PALAM:3 >= PALAMLV:3
			PRINT 젖은 
		IF TALENT:132
			PRINT 어린 균열에 
		ELSE
			PRINT 비렬에 
		ENDIF
		SIF TALENT:76 == 1
			PRINTFORM 극태 
		PRINTL 바이브 끝을 천천히 밀어넣었다…
		IF TFLAG:899 == 0
			PRINTFORM %타겟은()% 
			IF TALENT:0 && TALENT:85 && TALENT:76
				PRINT 자신이 처녀인 것도 잊은듯이 미소를 띄우고
			ELSEIF TALENT:0 && TALENT:85
				PRINTFORM %CALLNAME:PLAYER%에게 안타까운 시선을 보내고
			ELSEIF TALENT:0 && TALENT:76
				PRINT 처녀인 것도 상관없이, 허리를 흔들며 조르고
			ELSEIF TALENT:0 && TALENT:27
				PRINT 뭔가를 단념한듯한 표정을 짓고
				SIF TALENT:45 == 0 && TALENT:310 == 0
					PRINT  눈에는 희미하게 눈물을 띄우고
			ELSEIF TALENT:0
				PRINT 덜덜 떨며, 상실의 공포를 무서워하고
			ELSEIF EXP:0 >= 201 && TALENT:85
				PRINT 삽입에 대한 기대감만으로 허리를 떨며 가버릴 것처럼 되어
			ELSEIF EXP:0 >= 151 && EXP:0 <= 200 && TALENT:85
				PRINTFORM 녹을듯한 눈으로 %조사처리(CALLNAME:PLAYER,"를")% 응시하며, 스스로 음순을 벌리고
			ELSEIF EXP:0 >= 121 && EXP:0 <= 150 && TALENT:85
				PRINT 황홀한 표정으로 기다릴 수 없다는 듯 허리를 흔들고
			ELSEIF EXP:0 >= 71 && EXP:0 <= 120 && TALENT:85
				PRINTFORM 굉장히 갖고 싶다는듯이 눈물을 글썽이며 %조사처리(CALLNAME:PLAYER,"를")% 보고
			ELSEIF EXP:0 >= 41 && EXP:0 <= 70 && TALENT:85
				PRINT 바이브가 스치는 감촉에 완전히 황홀한 한숨을 흘리고
			ELSEIF EXP:0 >= 1 && EXP:0 <= 40 && TALENT:85
				PRINT 바이브가 스치는 감촉에 애타는듯이 애액을 흘리고
			ELSE
				PRINT 바이브가 스치는 감촉에 질주름을 움찔거리고
			ENDIF
			PRINTL  있다…
		ENDIF
		PRINTFORM %CALLNAME:TARGET%의 
		SIF PALAM:3 >= PALAMLV:4
			PRINT 젖어있는 
		PRINTFORML 성기에 바이브를 삽입했다
	ENDIF
;--------------------------------------------------------
;E마사지기
;--------------------------------------------------------
ELSEIF SELECTCOM == 12
	PRINTFORML %플레이어는()% %CALLNAME:TARGET%의 비소에 진동하고 있는 전동마사지기를 꽉 눌렀다…
	IF TFLAG:899 == 0
		PRINTFORM %타겟은()% 
		IF ABL:0 <= 2 && PALAM:5 >= PALAMLV:0 && PALAM:5 <= PALAMLV:3
			PRINT 굉장히 간지러운 듯하다
		ELSEIF ABL:0 <= 2 && PALAM:5 >= PALAMLV:3
			PRINT 아무 말도 못하고 있다
		ELSEIF TALENT:85 && ABL:0 >= 4 && PALAM:5 >= PALAMLV:0 && PALAM:5 <= PALAMLV:3
			PRINT 애타게 기다린 것처럼 다리를 벌리고, 받아들이고 있는 것 같다
		ELSEIF TALENT:85 && ABL:0 >= 4 && PALAM:5 >= PALAMLV:3 && PALAM:5 <= PALAMLV:5
			PRINTFORM 쾌락에 침을 흘리며, 몇번이나 %CALLNAME:PLAYER%의 이름을 불렀다
		ELSEIF TALENT:85 && ABL:0 >= 4 && PALAM:5 >= PALAMLV:5
			PRINTFORM %조사처리(CALLNAME:PLAYER,"를")% 끌어안고 절정의 황홀감에 잠겨있는 것 같다
		ELSEIF ABL:0 >= 3 && PALAM:5 >= PALAMLV:0 && PALAM:5 <= PALAMLV:3
			PRINT 강한 쾌감에 얼굴을 붉히고 있는 것 같다
		ELSEIF ABL:0 >= 3 && PALAM:5 >= PALAMLV:3 && PALAM:5 <= PALAMLV:5
			PRINT 정신이 나간듯한 눈으로 쾌락을 탐내고 있는 것 같다
		ELSEIF ABL:0 >= 3 && PALAM:5 >= PALAMLV:5
			PRINT 당장이라도 절정에 달할 것 같은 모습이다
		ENDIF
		PRINTL …
	ENDIF
;--------------------------------------------------------
;애널바이브
;--------------------------------------------------------
ELSEIF SELECTCOM == 13
	IF TEQUIP:13
		PRINTFORML %CALLNAME:TARGET%의 아누스에서 애널바이브를 뽑았다.
		IF TFLAG:899 == 0
			PRINTFORM %타겟은()% 
			IF EXP:1 >= 180
				PRINT 음순과 입에서 침을 흘리면서, 엉덩이를 벌리고 있다
			ELSEIF EXP:1 >= 141 && EXP:1 <= 179
				PRINT 젖어있는 눈동자로 애원하듯이 엉덩이를 흔들었다
			ELSEIF EXP:1 >= 101 && EXP:1 <= 140
				PRINT 아직 부족하다는듯이 엉덩이를 쓱 내밀었다
			ELSEIF EXP:1 <= 20
				PRINT 자극을 괴로운듯한 표정으로 참고 있다
			ELSEIF EXP:1 >= 21 && EXP:1 <= 40
				PRINT 강제적인 배설감에 얼굴을 찡그렸다
			ELSEIF EXP:1 >= 41 && EXP:1 <= 75
				PRINT 엉덩이를 우물쭈물 흔들면서도, 어딘지 안심한듯한 표정을 지었다
			ELSEIF EXP:1 >= 76 && EXP:1 <= 100
				PRINT 조금 아쉬운듯이 엉덩이를 흔들었다
			ENDIF
			PRINTFORML …
		ENDIF
	ELSE
		PRINTFORML %CALLNAME:TARGET%의 국혈에 애널바이브를 가져다댔다…
		IF TFLAG:899 == 0
			PRINTFORM %타겟은()% 
			IF EXP:1 >= 180
				PRINT 녹아버린 표정으로, 스스로의 엉덩이를 벌리고
			ELSEIF EXP:1 >= 141 && EXP:1 <= 179
				PRINT 삽입을 애타게 기다리는듯이 허리를 흔들고
			ELSEIF EXP:1 >= 101 && EXP:1 <= 140
				PRINT 뺨을 붉히고, 기대감으로 엉덩이를 흔들고
			ELSEIF EXP:1 <= 20
				PRINT 부저한 구멍을 범해지는 치욕과 굴욕감으로 얼룩진 얼굴을 하고
			ELSEIF EXP:1 >= 21 && EXP:1 <= 40
				PRINT 수치심으로 물든 얼굴을 숙이고
			ELSEIF EXP:1 >= 41 && EXP:1 <= 75
				PRINT 기대와 불안이 섞인듯한 복잡한 표정을 하고
			ELSEIF EXP:1 >= 76 && EXP:1 <= 100
				PRINT 바이브의 자극에 몸을 떨고
			ENDIF
			PRINTFORML  있다…
		ENDIF

		PRINTFORM %CALLNAME:TARGET%의 
		SIF PALAM:3 >= PALAMLV:4
			PRINT 점액투성이의 
		PRINTFORML 아누스에 바이브를 삽입했다
	ENDIF
;--------------------------------------------------------
;전동클리캡
;--------------------------------------------------------
ELSEIF SELECTCOM == 14
	IF TEQUIP:14
		PRINTFORML %CALLNAME:TARGET%에게서 전동 클리캡을 떼어냈다
	ELSE
		PRINTFORML %CALLNAME:TARGET%의 음핵에 전동 클리캡을 부착했다
	ENDIF
;--------------------------------------------------------
;ニプル클리캡
;--------------------------------------------------------
ELSEIF SELECTCOM == 15
	IF TEQUIP:15
		PRINTFORML %CALLNAME:TARGET%에게서 클립 로터를 떼어냈다
	ELSE
		PRINTFORML %CALLNAME:TARGET%의 유두에 클립 로터를 붙였다
	ENDIF
;--------------------------------------------------------
;착유기
;--------------------------------------------------------
ELSEIF SELECTCOM == 16
	IF TEQUIP:16
		PRINTFORML %CALLNAME:TARGET%에게서 착유기를 떼어냈다
	ELSE
		PRINTFORML %CALLNAME:TARGET%에게 착유기를 부착했다
	ENDIF
;--------------------------------------------------------
;오나홀
;--------------------------------------------------------
ELSEIF SELECTCOM == 17
	IF TEQUIP:17
		PRINTFORML %CALLNAME:TARGET%에게서 오나홀을 뗐다
	ELSE
		PRINTFORML %CALLNAME:TARGET%의 페니스를 오나홀에 삽입했다
	ENDIF
;--------------------------------------------------------
;샤워
;--------------------------------------------------------
ELSEIF SELECTCOM == 18
	IF TEQUIP:18
		PRINTFORML %CALLNAME:TARGET%에게 샤워를 그만두게했다
	ELSE
		PRINTFORML %CALLNAME:TARGET%에게 샤워하게 했다
	ENDIF
;--------------------------------------------------------
;애널비즈
;--------------------------------------------------------
ELSEIF SELECTCOM == 19
	IF TEQUIP:19
		PRINTFORML %CALLNAME:TARGET%의 아누스에서 애널비즈를 힘차게 뽑아냈다…
	ELSE
		PRINTFORML %CALLNAME:TARGET%의 국혈에 애널비즈를 천천히 집어넣었다…
	ENDIF
;--------------------------------------------------------
;정상위
;--------------------------------------------------------
ELSEIF SELECTCOM == 20
	PRINTFORM %플레이어는()% 
	;前回と今回の調教者が同じで, 前回の調教が정상위であり, 抜かないフラグがある場合
	IF TFLAG:60 && PREVCOM == 20 && ((ASSIPLAY && TFLAG:50) || (ASSIPLAY == 0 && TFLAG:50 == 0))
		;調教者がオトコか후타나리である
		IF TALENT:PLAYER:121 || TALENT:PLAYER:122
			PRINTFORML %CALLNAME:TARGET%의 질내의 감촉을 맛보면서, 다시 허리를 움직이기 시작했다…
		ELSE
		;페니스밴드を使用している場合
			PRINTFORML %CALLNAME:TARGET%의 질내에서 뽑지 않고, 다시 허리를 움직이기 시작했다…
		ENDIF
		SIF TFLAG:31
			TFLAG:31 = 2
	;同様に前回の調教が정상위以外のセックスか기승위の場合
	ELSEIF TFLAG:60 && (PREVCOM == 21 || PREVCOM == 22 || PREVCOM == 23 || PREVCOM == 34 || PREVCOM == 131 || PREVCOM == 132 || PREVCOM == 133 || PREVCOM == 134) && ((ASSIPLAY && TFLAG:50) || (ASSIPLAY == 0 && TFLAG:50 == 0))
		PRINTFORML 질내에서 빼지 않고 %타겟을()% 위를 향해 눕히고, 다시 허리를 움직이기 시작했다…
		SIF TFLAG:31
			TFLAG:31 = 2
	ELSE
		SIF TALENT:PLAYER:121 == 0 && TALENT:PLAYER:122 == 0	
			PRINTFORM 스스로의 허리에 장착한 페니스밴드로 
		PRINTFORM %CALLNAME:TARGET%의 
		SIF EXP:0 == 0
			PRINT 남자를 모르는 
		IF PALAM:3 >= PALAMLV:4
			PRINT 젖어있는 
		ENDIF
		IF TALENT:132
			PRINT 어린 균열을 
		ELSE
			PRINT 비렬을 
		ENDIF
		PRINTL 꿰뚫었다…
	ENDIF
;--------------------------------------------------------
;후배위
;--------------------------------------------------------
ELSEIF SELECTCOM == 21
	;獣姦かつ前回のコマンドも후배위で抜かないフラグがある場合
	IF TFLAG:60 && PREVCOM == 21 && TEQUIP:89
		PRINTFORML 개는 %CALLNAME:TARGET%의 둔부에 자신의 하반신을 밀착시키고, 계속 허리를 흔들고 있다……
		SIF ABL:39 >= 3 && PALAM:5 > PALAMLV:4
			PRINTFORML %CALLNAME:TARGET%도 개의 움직임에 맞춰 허리를 흔들고 있다
		SIF TFLAG:31
			TFLAG:31 = 2
	;獣姦の場合
	ELSEIF TEQUIP:89
		PRINTFORML %플레이어가()% 신호하자, 들개는 %CALLNAME:TARGET%의 몸을 뒤에서부터 덮쳐, 
		SIF EXP:0 == 0
			PRINT 남자를 모르는 
		IF PALAM:3 >= PALAMLV:4
			PRINT 젖어있는 
		ENDIF
		IF TALENT:132
			PRINT 어린 균열
		ELSE
			PRINT 비렬
		ENDIF
		PRINTFORML 의 가장 깊은 곳까지, 뿌리까지 붉게 부푼 개의 페니스를 찔러넣었다…
	;前回と今回の조교자が同じで, 前回の調教が후배위であり, 抜かないフラグがある場合
	ELSEIF TFLAG:60 && PREVCOM == 21 && ((ASSIPLAY && TFLAG:50) || (ASSIPLAY == 0 && TFLAG:50 == 0))
		;조교자が남자か후타나리である
		IF TALENT:PLAYER:119 || TALENT:PLAYER:121 || TALENT:PLAYER:122
			PRINTFORML %CALLNAME:TARGET%의 질내의 감촉을 맛보면서, 다시 허리를 움직이기 시작했다…
		ELSE
		;페니스밴드を使用している場合
			PRINTFORML %CALLNAME:TARGET%의 질내에서 뽑지 않고, 다시 허리를 움직이기 시작했다…
		ENDIF
		SIF TFLAG:31
			TFLAG:31 = 2
	;同様に, 前回の調教が후배위以外のセックスか기승위である場合
	ELSEIF TFLAG:60 && (PREVCOM == 20 || PREVCOM == 22 || PREVCOM == 23 || PREVCOM == 34) && ((ASSIPLAY && TFLAG:50) || (ASSIPLAY == 0 && TFLAG:50 == 0))
		PRINTFORML 질내에서 빼지 않고 %CALLNAME:TARGET%의 뒤로 돌아서, 다시 허리를 움직이기 시작했다…
		SIF TFLAG:31
			TFLAG:31 = 2
	ELSE
		PRINTFORM %플레이어는()%, 뒤에서 %CALLNAME:TARGET%의 
		SIF EXP:0 == 0
			PRINT 남자를 모르는 
		IF PALAM:3 >= PALAMLV:4
			PRINT 젖어있는 
		ENDIF
		IF TALENT:132
			PRINT 어린 균열
		ELSE
			PRINT 비렬
		ENDIF
		PRINTFORM 의 깊숙한 곳까지 
		IF TALENT:PLAYER:121 || TALENT:PLAYER:122	
			PRINT 딱딱하게 발기한 음경을 
		ELSE
			PRINT 허리에 고정한 페니스밴드를 
		ENDIF
		PRINTFORML 찔러넣었다…
	ENDIF
;--------------------------------------------------------
;대면좌위
;--------------------------------------------------------
ELSEIF SELECTCOM == 22
	PRINTFORM %플레이어는()% 
	;前回と今回の조교자が同じで, 前回の調教が대면좌위であり, 抜かないフラグがある場合
	IF TFLAG:60 && PREVCOM == 22 && ((ASSIPLAY && TFLAG:50) || (ASSIPLAY == 0 && TFLAG:50 == 0))
		PRINTFORML %타겟과()% 키스하며, 혀를 섞으면서 다시 허리를 움직이기 시작했다…
	;同様に前回の調教が정상위か기승위の場合
	ELSEIF TFLAG:60 && (PREVCOM == 20 || PREVCOM == 34) && ((ASSIPLAY && TFLAG:50) || (ASSIPLAY == 0 && TFLAG:50 == 0))
		PRINTFORML 질내에서 페니스를 빼지않고 %CALLNAME:TARGET%의 몸을 꽉 껴안고, 허리를 흔들기 시작했다…
	ELSE
		PRINTFORM %CALLNAME:TARGET%의 몸을 안아올려, 천천히 내리면서 
		SIF PALAM:3 >= PALAMLV:4
			PRINT 젖어있는 
		IF TALENT:132
			PRINT 어린 균열
		ELSE
			PRINT 비렬
		ENDIF
		PRINTFORML 에 삽입했다…
	ENDIF
;--------------------------------------------------------
;배면좌위
;--------------------------------------------------------
ELSEIF SELECTCOM == 23
	PRINTFORM %플레이어는()% 
	;수치 플레이중で, 前回と今回の조교자が同じで, 前回の調教が배면좌위であり, 抜かないフラグがある場合
	IF TEQUIP:57 && TFLAG:60 && PREVCOM == 23 && ((ASSIPLAY && TFLAG:50) || (ASSIPLAY == 0 && TFLAG:50 == 0))
			PRINTFORML %CALLNAME:TARGET%의 결합부와 가슴에 다시 손을 대어, 잘 보이도록 허리를 움직였다…
	;同様に, 수치 플레이중で前回の調教が후배위か기승위である場合
	ELSEIF TEQUIP:57 && TFLAG:60 && (PREVCOM == 21 || PREVCOM == 34) && ((ASSIPLAY && TFLAG:50) || (ASSIPLAY == 0 && TFLAG:50 == 0))
			PRINTFORML 질내에서 페니스를 빼지않고 %CALLNAME:TARGET%의 몸을 등 뒤에서 껴안고, 다리를 활짝 벌리게 했다…

	;通常プレイ中で, 前回と今回の조교자が同じで, 前回の調教が배면좌위であり, 抜かないフラグがある場合
	ELSEIF TFLAG:60 && PREVCOM == 23 && ((ASSIPLAY && TFLAG:50) || (ASSIPLAY == 0 && TFLAG:50 == 0))
		PRINTFORML %타겟을()% 등 뒤에서 꽉 껴안고, 다시 %CALLNAME:TARGET%의 몸을 흔들기 시작했다…
	;同様に, 前回の調教が후배위か기승위である場合
	ELSEIF TFLAG:60 && (PREVCOM == 21 || PREVCOM == 34) && ((ASSIPLAY && TFLAG:50) || (ASSIPLAY == 0 && TFLAG:50 == 0))
		PRINTFORML 질내에서 페니스를 빼지않고 %CALLNAME:TARGET%의 몸을 등 뒤에서 껴안고, 몸을 흔들기 시작했다…
	ELSEIF TEQUIP:57
			PRINTFORM %CALLNAME:TARGET%의 몸을 안아올려, 천천히 
			SIF PALAM:3 >= PALAMLV:4
				PRINT 젖어있는 
			IF TALENT:132
				PRINT 어린 균열
			ELSE
				PRINT 비렬
			ENDIF
			PRINTFORML 에 삽입하며, 다리를 활짝 벌리게했다…
	ELSE
		PRINTFORM %CALLNAME:TARGET%의 몸을 안아올려, 
		SIF PALAM:3 >= PALAMLV:4
			PRINT 젖어있는 
		IF TALENT:132
			PRINT 어린 균열
		ELSE
			PRINT 비렬
		ENDIF
		PRINTFORML 을 준비만전인 물건으로 천천히 관철했다…
	ENDIF
	IF TEQUIP:57
		IF ABL:17 >= 5
			PRINTFORML %타겟은()% 거울에 비치는 치태를 녹은 눈으로 응시하며 환희의 목소리를 내고 있다…
		ELSEIF ABL:17 >= 3
			PRINTFORML %타겟은()% 수치와 쾌락에 빠진듯한 표정으로 거울속의 치태를 응시하고 있다…
		ELSEIF ABL:17 >= 1
			PRINTFORML %타겟은()% 얼굴을 수치로 물들이면서 때때로 음란한 소리를 내고 있다…
		ELSE
			PRINTFORML %타겟은()% 손으로 얼굴을 가리고 수치심에 몸을 떨고 있다…
		ENDIF
	ENDIF
;--------------------------------------------------------
;정상위애널
;--------------------------------------------------------
ELSEIF SELECTCOM == 26
	PRINTFORM %플레이어는()% 
	;前回と今回の조교자が同じで, 前回の調教が정상위애널であり, 抜かないフラグがある場合
	IF TFLAG:60 && PREVCOM == 26 && ((ASSIPLAY && TFLAG:50) || (ASSIPLAY == 0 && TFLAG:50 == 0))
		;조교자が남자か후타나리である
		IF TALENT:PLAYER:121 || TALENT:PLAYER:122
			PRINTFORML %CALLNAME:TARGET%의 국혈의 감촉을 맛보면서 다시 허리를 움직이기 시작했다…
		ELSE
		;페니스밴드を使用している場合
			PRINTFORML %CALLNAME:TARGET%의 장내에서 뽑지않고, 다시 허리를 움직이기 시작했다…
		ENDIF
	;同様に前回の調教が후배위애널か기승위애널の場合
	ELSEIF TFLAG:60 && (PREVCOM == 27 || PREVCOM == 28 || PREVCOM == 36) && ((ASSIPLAY && TFLAG:50) || (ASSIPLAY == 0 && TFLAG:50 == 0))
		PRINTFORML 장내에서 빼내지 않고 %타겟을()% 위를 향해 눕히고, 다시 허리를 움직이기 시작했다…
	ELSE
		PRINTFORM %CALLNAME:TARGET%의 
		SIF PALAM:3 >= PALAMLV:4
			PRINT 점액 투성이의 
		PRINTL 아누스를 관철했다…
	ENDIF
;--------------------------------------------------------
;후배위애널
;--------------------------------------------------------
ELSEIF SELECTCOM == 27
	SIF TEQUIP:89 == 0
		PRINTFORM %플레이어는()% 
	;前回と今回の조교자が同じで, 前回の調教が후배위애널であり, 抜かないフラグがある場合
	IF TFLAG:60 && PREVCOM == 27 && ((ASSIPLAY && TFLAG:50) || (ASSIPLAY == 0 && TFLAG:50 == 0))
		;수간 플레이중
		IF TEQUIP:89
			PRINTFORML 개는 %CALLNAME:TARGET%의 둔부에 자신의 하반신을 밀착시키고, 허리를 계속 흔들고 있다……
			SIF EXP:56 >= 200 && PALAM:5 > PALAMLV:4
				PRINTFORML %CALLNAME:TARGET%도 개의 움직임에 맞춰 허리를 흔들고 있다
		;조교자が남자か후타나리である
		ELSEIF TALENT:PLAYER:121 || TALENT:PLAYER:122
			PRINTFORML %CALLNAME:TARGET%의 아누스의 감촉을 맛보면서 다시 허리를 움직이기 시작했다…
		ELSE
		;페니스밴드を使用している場合
			PRINTFORML %CALLNAME:TARGET%의 장내에서 뽑지않고, 다시 허리를 움직이기 시작했다…
		ENDIF
		SIF TFLAG:31
			TFLAG:31 = 2
	;同様に, 前回の調教が정상위애널か기승위애널である場合
	ELSEIF TFLAG:60 && (PREVCOM == 26 || PREVCOM == 29 || PREVCOM == 36) && ((ASSIPLAY && TFLAG:50) || (ASSIPLAY == 0 && TFLAG:50 == 0))
		PRINTFORML 장내에서 빼내지 않고 %CALLNAME:TARGET%의 뒤로 돌아, 다시 허리를 움직이기 시작했다…
		SIF TFLAG:31
			TFLAG:31 = 2
	ELSE
		SIF TEQUIP:89
			PRINT 발정난 개가, 그 페니스를 
		PRINTFORM 등뒤에서 %CALLNAME:TARGET%의 
		SIF ABL:3 >= 3
			PRINT 충분히 개발된 
		SIF PALAM:3 >= PALAMLV:4
			PRINT 점액 투성이의 
		PRINTFORML 애널에 뿌리까지 삽입했다…
	ENDIF
;--------------------------------------------------------
;대면좌위애널
;--------------------------------------------------------
ELSEIF SELECTCOM == 28
	PRINTFORM %플레이어는()% 
	;前回と今回の조교자が同じで, 前回の調教が대면좌위であり, 抜かないフラグがある場合
	IF TFLAG:60 && PREVCOM == 28 && ((ASSIPLAY && TFLAG:50) || (ASSIPLAY == 0 && TFLAG:50 == 0))
		PRINTFORML %타겟과()% 키스하며, 혀를 섞으면서 다시 허리를 움직이기 시작했다…
	;同様に前回の調教が정상위, 기승위の場合
	ELSEIF TFLAG:60 && (PREVCOM == 26 || PREVCOM == 36) && ((ASSIPLAY && TFLAG:50) || (ASSIPLAY == 0 && TFLAG:50 == 0))
		PRINTFORML 장내에서 페니스를 빼지않고 %CALLNAME:TARGET%의 몸을 꽉 껴안고, 허리를 흔들기 시작했다…
	ELSE
		PRINTFORM %CALLNAME:TARGET%의 몸을 안아올려, 천천히 내리면서 
		SIF PALAM:3 >= PALAMLV:4
			PRINT 점액 투성이의 
		PRINTFORML 아누스에 삽입했다…
	ENDIF
;--------------------------------------------------------
;배면좌위애널
;--------------------------------------------------------
ELSEIF SELECTCOM == 29
	PRINTFORM %플레이어는()% 
	;前回と今回の조교자が同じで, 前回の調教が배면좌위であり, 抜かないフラグがある場合
	IF TFLAG:60 && PREVCOM == 29 && ((ASSIPLAY && TFLAG:50) || (ASSIPLAY == 0 && TFLAG:50 == 0))
		PRINTFORML %타겟을()% 등뒤에서 꽉 껴안고, 다시 %CALLNAME:TARGET%의 몸을 흔들기 시작했다…
	;同様に, 前回の調教が후배위か기승위である場合
	ELSEIF TFLAG:60 && (PREVCOM == 27 || PREVCOM == 36) && ((ASSIPLAY && TFLAG:50) || (ASSIPLAY == 0 && TFLAG:50 == 0))
		PRINTFORML 장내에서 페니스를 빼지않고 %CALLNAME:TARGET%의 몸을 등 뒤에서 껴안고, 몸을 흔들기 시작했다…
	ELSE
		PRINTFORM %CALLNAME:TARGET%의 몸을 안아올리고, 
		SIF PALAM:3 >= PALAMLV:4
			PRINT 점액 투성이가 된 
		PRINTFORML 아누스를 준비만전의 물건으로 천천히 관철했다…
	ENDIF
;--------------------------------------------------------
;수음
;--------------------------------------------------------
ELSEIF SELECTCOM == 30
	PRINTFORM %타겟은()% 
	SIF TALENT:85 && ASSIPLAY == 0 && TEQUIP:89 == 0
		PRINT 사랑스러운 듯이  
	SIF TALENT:76
		PRINT 스스로 
	;수간 플레이중
	IF TEQUIP:89
		PRINTFORM 개의 
	ELSE
		PRINTFORM %CALLNAME:PLAYER%의 
	ENDIF
	PRINTFORM 페니스를 
	IF CFLAG:42 == 83
		PRINTFORM 레이스 파티 글러브를 낀 
	ELSEIF CFLAG:42 == 85
		PRINTFORM 에나멜 롱 글러브를 낀 
	ENDIF
	PRINTFORML 손가락으로 문지르고 있다…
;--------------------------------------------------------
;펠라치오
;--------------------------------------------------------
ELSEIF SELECTCOM == 31
	PRINTFORM %타겟은()% 
	SIF TALENT:85 && ASSIPLAY == 0 && TEQUIP:89 == 0
		PRINT 사랑스러운 듯이  
	SIF TALENT:76
		PRINT 스스로 
	;수간 플레이중
	IF TEQUIP:89
		PRINTFORM 개의 
	ELSE
		PRINTFORM %CALLNAME:PLAYER%의 
	ENDIF
	PRINTFORML 페니스를 혀로 핥아 돌리고 있다…
;--------------------------------------------------------
;파이즈리
;--------------------------------------------------------
ELSEIF SELECTCOM == 32
	PRINTFORM %타겟은()% 
	IF (CFLAG:40 & 64) && CFLAG:42 <= 50
		CALL PRINT_CLOTHTYPE_SPECIAL
		PRINT  너머로, 
	ELSEIF CFLAG:40 & 28
		CALL PRINT_CLOTHTYPE_MAIN2
		PRINT  너머로, 
	ELSEIF (CFLAG:40 & 2)
		PRINT 속옷 너머로, 
	ENDIF
	
	SIF TALENT:85 && ASSIPLAY == 0
		PRINT 황홀한 표정을 지으며 
	IF TALENT:109
		PRINTFORM 평평한 가슴으로 열심히 
			;수간 플레이중
			IF TEQUIP:89
				PRINTFORM 개의 페니스를 
			ELSE
				PRINTFORM %CALLNAME:PLAYER%의 페니스를 
			ENDIF
	ELSEIF TALENT:110
		PRINTFORM 풍만한 가슴으로 
			;수간 플레이중
			IF TEQUIP:89
					PRINTFORM 개의 페니스를 감싸고 
				ELSE
					PRINTFORM %CALLNAME:PLAYER%의 페니스를 감싸고 
				ENDIF
	ELSEIF TALENT:114
		PRINTFORM 유방 사이에 완전히 묻힌 
			;수간 플레이중
			IF TEQUIP:89
					PRINTFORM 개의 페니스를 
				ELSE
					PRINTFORM %CALLNAME:PLAYER%의 페니스를 
				ENDIF
	ELSE
		PRINTFORM 가슴으로 
			;수간 플레이중
			IF TEQUIP:89
					PRINTFORM 개의 페니스를 감싸 
				ELSE
					PRINTFORM %CALLNAME:PLAYER%의 페니스를 감싸 
				ENDIF
	ENDIF
	PRINTL 문지르고 있다…
;--------------------------------------------------------
;스마타
;--------------------------------------------------------
ELSEIF SELECTCOM == 33
	PRINTFORM %타겟은()% %CALLNAME:PLAYER%의 페니스를 사타구니 사이에 끼우고 
	IF TALENT:85 && TALENT:76 && PALAM:5 > PALAMLV:5
		PRINTFORM 쾌락에 빠진 표정으로 %조사처리(CALLNAME:PLAYER,"를")% 응시하면서 
	ELSEIF TALENT:85 && PALAM:5 > PALAMLV:5
		PRINT 금방이라도 녹아버릴듯한 표정을 지으면서 
	ELSEIF TALENT:76 && PALAM:5 > PALAMLV:5
		PRINTFORM 애원하는듯한 시선으로 
	ELSEIF PALAM:5 > PALAMLV:4
		PRINT 입에서 침을 흘리면서 
	ELSEIF TALENT:85 && TALENT:76
		PRINTFORM 완전히 도취된 눈동자로 %조사처리(CALLNAME:PLAYER,"를")% 응시하면서 
	ELSEIF TALENT:85
		PRINT 행복한 표정을 띄우면서 
	ELSEIF TALENT:76
		PRINTFORM 음탕한 눈초리로 도발하면서 
	ELSEIF TALENT:36
		PRINTFORM 대담하게 허리를 움직이면서 
	ELSEIF TALENT:22
		PRINTFORM 상기된 얼굴로 
	ELSE
		PRINT 부끄러운듯이 
	ENDIF
	IF TALENT:132
		PRINT 어린 균열을 
	ELSE
		PRINT 음순을 
	ENDIF
	PRINTL 페니스에 문지르고 있다…
;--------------------------------------------------------
;기승위
;--------------------------------------------------------
ELSEIF SELECTCOM == 34
	;獣姦かつ前回のコマンドも기승위で抜かないフラグがある場合
	IF TFLAG:60 && PREVCOM == 34 && TEQUIP:89
		PRINTFORML %타겟은()% 난폭한 숨을 내쉬며 개 위에서 허리를 계속 흔들고 있다……
		SIF TFLAG:31
			TFLAG:31 = 2
	;獣姦の場合
	ELSEIF TEQUIP:89
		PRINTFORML %타겟은()% 배를 들어낸 개 위에 스스로 올라타, 
		PRINTFORML 들개의 발기한 페니스를 자신의 의사로 바기너에 삽입했다……
	;前回と今回の조교자が同じで, 前回の調教が기승위であり, 抜かないフラグがある場合
	ELSEIF TFLAG:60 && PREVCOM == 34 && ((ASSIPLAY && TFLAG:50) || (ASSIPLAY == 0 && TFLAG:50 == 0))
		PRINTFORML %타겟은()% 삽입된채로 페니스를 조이고, 다시 허리를 움직이기 시작했다…
		SIF TFLAG:31
			TFLAG:31 = 2
	;同様に, 前回の調教が정상위か후배위である場合
	ELSEIF TFLAG:60 && (PREVCOM == 20 || PREVCOM == 21 || PREVCOM == 24 || PREVCOM == 25 || PREVCOM == 26) && ((ASSIPLAY && TFLAG:50) || (ASSIPLAY == 0 && TFLAG:50 == 0))
		PRINTFORML %플레이어는()% 질내에서 빼지않고 %타겟을()% 안아올리며, 아래에서 허리를 찔러올렸다…
		SIF TFLAG:31
			TFLAG:31 = 2
	ELSE
		PRINTFORM %타겟은()% %CALLNAME:PLAYER%의 위에 올라타, 느긋하게 허리를 내
		IF TALENT:132 && EXP:0 == 0
			PRINT 려, 남자를 모르는 어린 균열에 페니스를 삼켜갔다…
		ELSEIF EXP:0 == 0
			PRINT 려, 남자를 모르는 비렬에 페니스를 삼켜갔다…
		ELSE
			PRINT 렸다…
		ENDIF
		IF EXP:0 >= EXPLV:3
			PRINTL  
			PRINTFORM %타겟은()% 
			IF TALENT:99
				PRINT 덮치듯이 짓누르며 
			ELSEIF TALENT:100
				PRINT 작은 몸으로 열심히 
			ENDIF
			SIF TALENT:153 && CFLAG:110 <= DAY+10
				PRINT 부풀어 오른 배를 끌어안은채로 
			SIF TALENT:110 || TALENT:114
				PRINT 커다란 가슴을 흔들면서 
			PRINT 스스로 허리를 움직이고 있다…
		ENDIF
		PRINTL  
	ENDIF
;--------------------------------------------------------
;거품춤
;--------------------------------------------------------
ELSEIF SELECTCOM == 35
	PRINTFORM %타겟은()% 비누로 거품을 만들어 자신의 몸에 바르고, 
	SIF TALENT:85
		PRINT 황홀한 표정으로 
	IF TALENT:99
		PRINTFORM %조사처리(CALLNAME:PLAYER,"를")% 감싸듯이 
	ELSEIF TALENT:100
		PRINTFORM %CALLNAME:PLAYER%에게 작은 몸을 힘껏 밀착해 
	ELSEIF TALENT:110 || TALENT:114
		PRINTFORM %CALLNAME:PLAYER%에게 풍만한 가슴과 매끈한 피부를 문지르며 
	ELSE
		PRINTFORM %CALLNAME:PLAYER%에게 전신을 밀착해 
	ENDIF
	PRINTL 애무했다…
;--------------------------------------------------------
;기승위애널
;--------------------------------------------------------
ELSEIF SELECTCOM == 36
	;獣姦かつ前回のコマンドも기승위で抜かないフラグがある場合
	IF TFLAG:60 && PREVCOM == 36 && TEQUIP:89
		PRINTFORML %타겟은()% 난폭한 숨을 내쉬며 개 위에서 허리를 계속 흔들고 있다……
	;獣姦の場合
	ELSEIF TEQUIP:89
		PRINTFORML %타겟은()% 위로 향한 개 위에 스스로 올라타, 
		PRINTFORML 들개의 발기한 페니스를 자신의 의사로 아누스에 삽입했다……
	;前回と今回の조교자が同じで, 前回の調教が기승위であり, 抜かないフラグがある場合
	ELSEIF TFLAG:60 && PREVCOM == 36 && ((ASSIPLAY && TFLAG:50) || (ASSIPLAY == 0 && TFLAG:50 == 0))
		PRINTFORML %타겟은()% 삽입된채로 페니스를 조이고, 다시 허리를 움직이기 시작했다…
	;同様に, 前回の調教が정상위か후배위である場合
	ELSEIF TFLAG:60 && (PREVCOM == 26 || PREVCOM == 22 || PREVCOM == 28 || PREVCOM == 29) && ((ASSIPLAY && TFLAG:50) || (ASSIPLAY == 0 && TFLAG:50 == 0))
		PRINTFORML %플레이어는()% 장내에서 빼지않고 %타겟을()% 안아올리고, 아래에서 허리를 찔러올렸다…
	ELSE
		PRINTFORM %타겟은()% %CALLNAME:PLAYER%의 위에 올라타, 
		SIF PALAM:3 >= PALAMLV:4
			PRINT 점액 투성이의 
			PRINTL 아누스로 천천히 물건을 삼켜나갔다…
	ENDIF
	IF EXP:0 >= EXPLV:3
		PRINTFORM %타겟은()% 
		IF TALENT:99
			PRINT 덮치듯이 짓누르며 
		ELSEIF TALENT:100
			PRINT 작은 몸으로 열심히 
		ENDIF
	SIF TALENT:110 || TALENT:114
		PRINT 크게 가슴을 흔들면서 
		PRINTL 스스로 허리를 움직이고 있다…
	ENDIF
;--------------------------------------------------------
;애널봉사
;--------------------------------------------------------
ELSEIF SELECTCOM == 37
	;수간 플레이중
	IF TEQUIP:89
		PRINTFORML %타겟은()% 개의 애널에 입맞추고 있다…
	ELSEIF TALENT:85 == 1
		PRINTFORML %플레이어가()% 명령하자 %타겟은()% 기쁜 표정으로 %CALLNAME:PLAYER%의 애널에 입을 맞추었다
	ELSE
		PRINTFORML %플레이어는()% %CALLNAME:TARGET%에게 자신의 애널에 입맞추게했다…
	ENDIF
;--------------------------------------------------------
;풋잡
;--------------------------------------------------------
ELSEIF SELECTCOM == 38
	PRINTFORM %타겟은()% %CALLNAME:PLAYER%의 페니스를 
	;IF TALENT:87 && (CFLAG:40 & 1) && TALENT:122 == 0
	IF (CFLAG:40 & 1) && TALENT:122 == 0
		SIF PALAM:3 >= PALAMLV:4
			PRINT 애액으로 비렬의 모양이 찍힌 
		CALL PRINT_UNDERWEAR_UNDER
		PRINTFORML %조사만처리(RESULT+2,"를")% 도발하듯이 보여주며 
	;ELSEIF TALENT:87 && (CFLAG:40 & 1) == 0 && TALENT:122 == 0
	ELSEIF !(CFLAG:40 & 1) && TALENT:122 == 0
		SIF PALAM:3 >= PALAMLV:4
			PRINT 애액으로 흠뻑 젖은채 입을 벌린 
		SIF (CFLAG:7 & 4)
			PRINTFORM 거울상처럼 두개의 피어스가 빛나는 
		IF CFLAG:608 == 0
			PRINTFORM 예쁜 핑크색인 
		ELSEIF CFLAG:608 == 1
			PRINTFORM 라비아가 비대화된  
		ELSEIF CFLAG:608 == 2
			PRINTFORM 라비아가 검게 변색되고 비대화된 
		ENDIF
		PRINTFORML
		IF FLAG:36 
			IF CFLAG:6 > 18
				PRINT 음부에서 애널까지 음모로 뒤덮인 
			ELSEIF CFLAG:6 > 13
				PRINT 음모가 진하게 난 
			ELSEIF CFLAG:6 > 10
				PRINT 음모를 예쁘게 정돈한 
			ELSEIF CFLAG:6 > 8
				PRINT 가는 음모가 나있는 
			ELSEIF CFLAG:6 > 6
				PRINT 희미하게 음모가 나기 시작한 
			ELSEIF CFLAG:6 > 0
				PRINT 맨들맨들한 
			ENDIF
			PRINTFORM 성기를 
		ELSE
			PRINTFORM 성기를 
		ENDIF
		PRINTFORM 도발하듯이 보여주며 
	ENDIF
	IF CFLAG:173 == - 1
		PRINTFORM 맨발로 
	ELSEIF CFLAG:173 == 0
		CALL PRINT_SHOESTYPE_MAIN
		PRINTFORM  너머로 
	ENDIF
	SIF TALENT:83 || ABL:20 > 2
		PRINT 짓밟듯이 
	SIF TALENT:85 || TALENT:76
		PRINT 열심히 
	PRINTL 문지르고 있다…
;--------------------------------------------------------
;오나홀애무
;--------------------------------------------------------
ELSEIF SELECTCOM == 39
	PRINTFORM %타겟은()% 
	SIF ABL:20 <= 2
		PRINTFORM %플레이어가()% 시키는 대로 
	IF ASSIPLAY == 0 && TEQUIP:89 == 0 && TALENT:83 == 0 && TALENT:85 == 1
		PRINT 곤혹해하면서 
	ELSEIF ASSIPLAY == 0 && TEQUIP:89 == 0 && TALENT:83 == 1 && TALENT:85 == 1
		PRINTFORM 질려하고는 %플레이어를()% 무시하듯 바라보며 
	ELSEIF ASSIPLAY == 0 && TEQUIP:89 == 0 && TALENT:83 == 1 && TALENT:85 == 0
		PRINTFORM 질려하고는 %플레이어를()% 바라보며 
	ENDIF
	;수간 플레이중
	IF TEQUIP:89
		PRINTFORM 개의 
	ELSE
		PRINTFORM %CALLNAME:PLAYER%의 
	ENDIF
	PRINTFORML 페니스를 오나홀로 문지르고 있다…
;追加ここから
;--------------------------------------------------------
;나이즈리
;--------------------------------------------------------
ELSEIF SELECTCOM == 141
	PRINTFORM %타겟은()% 
	IF (CFLAG:40 & 64) && CFLAG:42 <= 50
		CALL PRINT_CLOTHTYPE_SPECIAL
		PRINT  너머로, 
	ELSEIF CFLAG:40 & 28
		CALL PRINT_CLOTHTYPE_MAIN2
		PRINT  너머로, 
	ELSEIF (CFLAG:40 & 2)
		PRINT 속옷 너머로, 
	ENDIF
	
	SIF TALENT:85 && ASSIPLAY == 0
		PRINT 황홀한 표정으로 
	IF TALENT:413
		PRINT 없는 가슴을 열심히 
	ELSEIF TALENT:TALENT:122
		PRINT 없는 가슴을 열심히 
	ELSE
		PRINT 없는 가슴을 열심히 
	ENDIF
	PRINTFORML %CALLNAME:PLAYER%의 페니스에 비비고 있다…

;-------------------------------------------------
;세로파이즈리
;-------------------------------------------------
ELSEIF SELECTCOM == 142
	PRINTFORM %타겟은()% 
	;연심
	SIF TALENT:85
		PRINT 황홀한 표정을 지으며 
		;거유
		IF TALENT:110
			PRINT 풍만한 가슴으로 페니스를 감싸고 흔들어 
		;폭유
		ELSEIF TALENT:114
			PRINT 너무 풍만한 가슴으로 페니스를 감싸고 주물러 
		ENDIF
		PRINTW 자극하고 있다……
					;追加ここまで
;--------------------------------------------------------
;스팽킹
;--------------------------------------------------------
ELSEIF SELECTCOM == 40
	PRINTFORM %플레이어는()%
	IF CFLAG:42 == 11 && (CFLAG:40 & 64)
		CALL PRINT_CLOTHTYPE_SPECIAL
		PRINTFORM  위를 
	ELSE
		PRINTFORM %CALLNAME:TARGET%의 엉덩이를 
	ENDIF
	PRINTFORML 손바닥으로 때렸다
	IF CFLAG:42 == 11 && (CFLAG:40 & 64)
		CALL PRINT_CLOTHTYPE_SPECIAL
		PRINTFORML %조사만처리(RESULT+2,"를")% 입고 있는 %CALLNAME:TARGET%에게는, 그다지 효과가 없는 것 같다…
	ELSEIF PREVCOM == 40 && (CFLAG:40 & 16) == 0
		PRINTFORML %CALLNAME:TARGET%의 얻어맞은 부분이 더 붉어져간다…
	ELSEIF (CFLAG:40 & 16) == 0
		PRINTFORML %CALLNAME:TARGET%의 얻어맞은 부분이 붉어졌다…
	ENDIF
;--------------------------------------------------------
;채찍
;--------------------------------------------------------
ELSEIF SELECTCOM == 41
	PRINTFORM %플레이어는()% 
	IF CFLAG:42 == 11 && (CFLAG:40 & 64)
		CALL PRINT_CLOTHTYPE_SPECIAL
	ELSE
		PRINTFORM %CALLNAME:TARGET%의 몸
	ENDIF
	PRINTFORML 에 채찍을 내리쳤다…
	IF CFLAG:42 == 11 && (CFLAG:40 & 64)
		CALL PRINT_CLOTHTYPE_SPECIAL
		PRINTFORML %조사만처리(RESULT+2,"를")% 입고 있는 %CALLNAME:TARGET%에게는, 그다지 효과가 없는 것 같다…
	ELSEIF PREVCOM == 41
		PRINTFORML %CALLNAME:TARGET%의 몸에 채찍 자국이 점점 늘어난다…
	ELSE
		PRINTFORML %CALLNAME:TARGET%의 몸에 새빨간 채찍 자국이 생겼다…
	ENDIF
;--------------------------------------------------------
;바늘
;--------------------------------------------------------
ELSEIF SELECTCOM == 42
	PRINTFORM %플레이어는()% 
	IF CFLAG:42 == 11 && (CFLAG:40 & 64)
		CALL PRINT_CLOTHTYPE_SPECIAL
		PRINTFORML %조사만처리(RESULT+2,"를")% 바늘로 찔렀다…
	ELSE
		PRINTFORML %CALLNAME:TARGET%의 몸을 바늘로 찔렀다…
	ENDIF
	IF CFLAG:42 == 11 && (CFLAG:40 & 64)
		CALL PRINT_CLOTHTYPE_SPECIAL
		PRINTFORML %조사만처리(RESULT+2,"를")% 입고 있는 %CALLNAME:TARGET%에게는, 그다지 효과가 없는 것 같다…
	ENDIF
;--------------------------------------------------------
;배빵
;--------------------------------------------------------
ELSEIF SELECTCOM == 144
	PRINTFORM %플레이어는()% 
	IF (CFLAG:40 & 4) == 0
		PRINTFORM 드러나있는 
	ELSE
		CALL PRINT_CLOTHTYPE_MAIN2
		PRINTFORM 의 위에서 
	ENDIF
	PRINTFORML %CALLNAME:TARGET%의 배를 힘껏 때렸다…
	IF PREVCOM == 144
		PRINTFORML %타겟은()% 더해지는 고통에 눈물과 침을 흘리며 토하고 있다…
	ELSE
		PRINTFORML %타겟은()% 고통에 소리를 지르며, 배를 움켜쥐고 침대 위에서 몸을 웅크리고 있다…
	ENDIF
;--------------------------------------------------------
;아이마스크
;--------------------------------------------------------
ELSEIF SELECTCOM == 43
	IF TEQUIP:43
		PRINTFORML %CALLNAME:TARGET%에게서 아이마스크를 떼어주었다
	ELSE
		PRINTFORML %CALLNAME:TARGET%에게 아이마스크를 씌웠다
	ENDIF
;--------------------------------------------------------
;밧줄
;--------------------------------------------------------
ELSEIF SELECTCOM == 44
	PRINTFORM %플레이어는()% 
	IF CFLAG:42 == 11 && (CFLAG:40 & 64)
		CALL PRINT_CLOTHTYPE_SPECIAL
		IF TEQUIP:44
			PRINTFORML %조사만처리(RESULT+2,"를")% 풀어주었다
		ELSE
			PRINTFORML %조사만처리(RESULT+2,"를")% 밧줄로 묶었다
		ENDIF
	ELSE
		PRINTFORM %CALLNAME:TARGET%
		IF TEQUIP:44
			PRINTFORML %조사만처리(CALLNAME:TARGET,"를")% 풀어주었다
		ELSE
			PRINTFORML %조사만처리(CALLNAME:TARGET,"를")% 밧줄로 묶었다
		ENDIF
	ENDIF
;--------------------------------------------------------
;볼개그
;--------------------------------------------------------
ELSEIF SELECTCOM == 45
	IF TEQUIP:45
		PRINTFORML %CALLNAME:TARGET%에게서 볼재갈을 벗겨주었다
	ELSE
		PRINTFORML %CALLNAME:TARGET%에게 볼재갈을 씌웠다
	ENDIF
;--------------------------------------------------------
;관장기+플러그
;--------------------------------------------------------
ELSEIF SELECTCOM == 46
	IF TEQUIP:46
		PRINTFORML %타겟은()% 항문 마개를 뽑혀, 장내의 오물을 마구 흩뿌렸다.
		IF TFLAG:899 == 0
			IF ABL:21 == 0
				PRINTFORML %타겟은()% 고통과 치욕의 표정을 띄우고 있다
			ELSEIF ABL:21 == 1
				PRINTFORML %타겟은()% 비지땀을 흘리며, 아직도 하복부에 남아있는 배설감을 견디고 있다
			ELSEIF ABL:21 == 2
				PRINTFORML %타겟은()% 부끄러워하면서도, 해방감으로 안심한듯한 표정을 띄우고 있다.
			ELSEIF ABL:21 == 3
				PRINTFORML %타겟은()% 항문을 지나는 배설물의 감각에 때때로 음란한 허덕임을 지르고 있다.
			ELSEIF ABL:21 == 4
				PRINTFORML %타겟은()% 몽롱한 표정으로 배설감을 즐기고 있다.
			ELSEIF ABL:21 >= 5
				PRINTFORML %타겟은()% 배설감과 치욕을 마음껏 맛보고 있다.
				PRINTFORML %CALLNAME:TARGET%의 표정은 쾌감으로 야무지지 못하게 흐트러져, 항문에서 오물을 흘리는 것과 동시에, 질구에서도 애액을 뚝뚝 떨어트리고 있다.
			ENDIF
		ENDIF
	ELSE
		PRINTFORML %CALLNAME:TARGET%의 항문에 관장액을 흘려넣고, 마개를 했다
	ENDIF
;--------------------------------------------------------
;본디지 착용
;--------------------------------------------------------
ELSEIF SELECTCOM == 47
	IF TEQUIP:47
		PRINTFORML %CALLNAME:ASSI%의 본디지룩을 벗겼다…
	ELSE
		PRINTFORML %CALLNAME:ASSI%의 맨몸에 가죽제의 본디지룩을 입혔다…
	ENDIF

;--------------------------------------------------------
;풋잡한다
;--------------------------------------------------------
ELSEIF SELECTCOM == 48
	PRINTFORM %플레이어는()% %CALLNAME:TARGET%의 
	SIF PALAM:5 >= PALAMLV:3
		PRINT 딱딱하게 발기한 
	PRINTL 페니스를 다리로 문질렀다…

;--------------------------------------------------------
;애널전극
;--------------------------------------------------------
ELSEIF SELECTCOM == 49
	IF TEQUIP:49
		PRINTFORML %CALLNAME:TARGET%의 아누스에서 전극을 뽑아주었다…
	ELSE
		PRINTFORML %CALLNAME:TARGET%의 국혈에 전극을 삽입하며 스위치를 넣었다…
	ENDIF
;--------------------------------------------------------
;로션
;--------------------------------------------------------
ELSEIF SELECTCOM == 50
	CALL PRINT_CLOTHTYPE
	PRINTFORML 의 %CALLNAME:TARGET%에게 로션을 마구 칠했다…
;--------------------------------------------------------
;しあわせ草
;--------------------------------------------------------
ELSEIF SELECTCOM == 51
	IF TEQUIP:90
		PRINTFORML 최음효과가 있는 액체가 촉수에서 흘러나왔다
	ELSE
		PRINTFORM %플레이어는()% %CALLNAME:TARGET%에게 
		SIF ABL:PLAYER:12 >= 3
			PRINT 입으로 
		SIF ABL:10 <= 2
			PRINT 억지로 
		PRINTFORML 미약을 먹였다
	ENDIF
	PRINTFORML %타겟은()% 난폭한 숨을 토하며 피부를 상기시키고 있다…
;--------------------------------------------------------
;이뇨제
;--------------------------------------------------------
ELSEIF SELECTCOM == 52
	IF TEQUIP:90
		PRINTFORML 극세사의 촉수가, 이뇨 작용이 있는 액체를 요도로 직접 흘려넣었다…
	ELSE
		;顔面騎乗中
		IF TEQUIP:55 == 1
			PRINTFORML %타겟은()% 이뇨제를 입에 머금었다
		ELSE
			PRINTFORM %플레이어는()% %CALLNAME:TARGET%에게 
			SIF ABL:PLAYER:12 >= 3
				PRINT 입으로 
			SIF ABL:10 <= 2
				PRINT 억지로 
			PRINTFORML 미약을 먹였다
		ENDIF
	ENDIF
	IF TFLAG:899 == 0
		PRINTFORM %타겟은()% 난폭한 숨을 내쉬며, 
		SIF TALENT:45 == 0 && TALENT:310 == 0
			PRINTFORM 눈물을 띄우고 
		PRINTFORML 뇨의를 참고 있다…
	ENDIF
;--------------------------------------------------------
;비디오카메라
;--------------------------------------------------------
ELSEIF SELECTCOM == 53
	IF TEQUIP:53
		PRINTL ★★★비디오 촬영을 종료합니다★★★
	ELSE
		PRINTL ★★★비디오 촬영을 시작합니다★★★
	ENDIF
;--------------------------------------------------------
;야외 플레이
;--------------------------------------------------------
ELSEIF SELECTCOM == 54
	IF TEQUIP:54 == 0
		PRINTFORML 방으로 돌아왔다…
	ELSE
		CALL PRINT_CLOTHTYPE
		PRINTFORM 의 %CALLNAME:TARGET%
		IF ASSI > 0
			;同じ格好
			IF CFLAG:40 == CFLAG:ASSI:40 && CFLAG:41 == CFLAG:ASSI:41 && CFLAG:42 == CFLAG:ASSI:42
				PRINTFORM %조사만처리(CALLNAME:TARGET,"와")% %조사처리(CALLNAME:ASSI,"를")% 
			ELSE
				PRINTFORM %조사만처리(CALLNAME:TARGET,"와")% 
				T = TARGET
				TARGET = ASSI
				CALL PRINT_CLOTHTYPE
				TARGET = T
				PRINTFORM 의 %조사처리(CALLNAME:ASSI,"를")% 
			ENDIF
		ELSE
			PRINTFORM %조사만처리(CALLNAME:TARGET,"를")% 
		ENDIF
		;야외
		IF TEQUIP:54 == 1
			PRINTFORML 데리고 밖으로 나갔다…
		;教室
		ELSEIF TEQUIP:54 == 2
			PRINT 데리고 교실 셋트로 향했다…
		;プール
		ELSEIF TEQUIP:54 == 3
			PRINTL 데리고 수영장으로 갔다…
		ENDIF
	ENDIF
;--------------------------------------------------------
;대화한다
;--------------------------------------------------------
ELSEIF SELECTCOM == 56
	C = NO:TARGET + 2000
	;口上が存在すればそちらを優先
	IF FLAG:C
		PRINTFORML %플레이어는()% %CALLNAME:TARGET%에게 말을 건넸다…
		IF ABL:10 >= 3 && ABL:17 >= 2
			SIF CFLAG:8 != 1
				CALL GENITAL_SLANG
		ENDIF
	;비디오 촬영중なら자기소개
	ELSEIF TEQUIP:53
		PRINTFORML %플레이어가()% %CALLNAME:TARGET%에게 자기소개를 재촉하면, 
		IF TALENT:89 || ABL:17 >= 5
			PRINTFORM %타겟은()% 자신의 본명이나 지금까지의 성경험에 대해
			SIF ABL:31 >= 3
				PRINTFORM , 게다가 자위시에 하는 망상의 내용까지도
			PRINTFORML  기쁨에 차 말하기 시작했다……
			PRINTFORML 이 비디오를 모르는 사람이 본다는 기대만으로 사타구니를 적시고 있다……
			TFLAG:32 |= 2
		ELSEIF PALAM:5 >= PALAMLV:4 && (TALENT:76 || ABL:11 >= 5)
			PRINTFORML %타겟은()% 카메라를 보며 음란한 말을 하기 시작했다
			TFLAG:32 |= 2
		ELSEIF TALENT:85 || ABL:10 >= 3 || ABL:11 >= 4 || ABL:17 >= 2
			PRINTFORML %타겟은()% 카메라를 보며 자기소개를 시작했다
			TFLAG:32 |= 2
		ELSE
			PRINTFORML %타겟은()% 고개를 돌리고 아무말도 하지 않았다
		ENDIF
	ELSE
		PRINTFORM %플레이어가()% 
		IF PALAM:5 >= PALAMLV:4 && (TALENT:85 || ABL:10 >= 5) && TFLAG:60
			PRINTFORML 말을 걸자, %타겟은()% 허리를 흔들면서 사랑의 말을 외쳤다
		ELSEIF PALAM:5 >= PALAMLV:4 && (TALENT:76 || ABL:11 >= 5) && TFLAG:60
			PRINTFORML 말을 걸자, %타겟은()% 허리를 흔들면서 음란한 말을 계속해서 외쳤다
		ELSEIF (PALAM:4 >= PALAMLV:4 || ABL:10 >= 5 || TALENT:85) && PALAM:5 >= PALAMLV:4
			PRINTFORM 말을 걸자, %타겟은()% 
			IF TEQUIP:11 || TEQUIP:13 || TEQUIP:14 || TEQUIP:15 || TEQUIP:16 || TEQUIP:17
				PRINT 쾌락때문에 
			ELSEIF TEQUIP:44 || TEQUIP:49
				PRINT 고통때문에 
			ENDIF
			PRINTFORML 소리를 지르며, 필사적으로 대답했다
		ELSEIF PALAM:4 >= PALAMLV:4 || TALENT:85 || ABL:10 >= 5
			PRINTFORML 말을 걸자, %타겟은()% 기쁘게 대답했다
		ELSEIF PALAM:4 >= PALAMLV:2 ||  ABL:10 >= 3
			PRINTFORML 말을 걸자, %타겟은()% 흠칫흠칫 대답했다
		ELSE
			PRINTFORML 말을 걸었지만, %타겟은()% 제대로 듣고 있지 않은 것 같다…
		ENDIF
		IF ABL:10 >= 3 && ABL:17 >= 2
			SIF CFLAG:8 != 1
				CALL GENITAL_SLANG
		ENDIF
	ENDIF
	C = 0
;--------------------------------------------------------
;수치 플레이
;--------------------------------------------------------
ELSEIF SELECTCOM == 57
	IF TEQUIP:57
		PRINTFORML %타겟을()% 데리고 거울 앞에서 멀어졌다…
	ELSE
		CALL PRINT_CLOTHTYPE
		PRINTFORML 의 %타겟을()% 거울 앞에 데려왔다…
	ENDIF
;--------------------------------------------------------
;목욕탕 플레이
;--------------------------------------------------------
ELSEIF SELECTCOM == 58
	IF TEQUIP:58
		PRINTFORML %플레이어와()% %타겟은()% 방으로 돌아왔다…
	ELSE
		PRINTFORML %플레이어는()% %타겟을()% 욕실로 데려갔다…
	ENDIF
;--------------------------------------------------------
;코스프레
;--------------------------------------------------------
ELSEIF SELECTCOM == 59
	IF TEQUIP:59
		PRINTFORML %CALLNAME:TARGET%에게서 코스프레 의상을 벗겼다…
	ELSE
		PRINTFORMW %CALLNAME:TARGET%에게 코스프레 시키기로했다…
		CALL COSPLAY_COSTUME
	ENDIF
;--------------------------------------------------------
;조수한테키스
;--------------------------------------------------------
ELSEIF SELECTCOM == 60
	PRINTFORML %조수와()% %타겟은()% 혀를 섞으며 키스하고 있다…
;--------------------------------------------------------
;커닐링구스강제
;--------------------------------------------------------
ELSEIF SELECTCOM == 61
	PRINTFORML %CALLNAME:TARGET%에게 %CALLNAME:PLAYER%의 음순을 빨게했다…
;--------------------------------------------------------
;조수를 범한다
;--------------------------------------------------------
ELSEIF SELECTCOM == 62
	;前回の調教が조수를 범한다であり, 抜かないフラグがある場合
	IF TFLAG:60 && PREVCOM == 62
		PRINTFORML %CALLNAME:TARGET%에게 과시하면서, %조수를()% 계속해서 범했다…
	ELSE
		PRINTFORM %CALLNAME:TARGET%에게 과시하면서, %CALLNAME:ASSI%의 
		IF EXP:ASSI:0 == 0
			PRINT 처음을 
		ELSE
			PRINT 바기너를 
		ENDIF
		PRINTL 꿰뚫었다…
	ENDIF
;--------------------------------------------------------
;조개맞대기
;--------------------------------------------------------
ELSEIF SELECTCOM == 63
	PRINTFORML %플레이어와()% %타겟은()% 서로의 성기를 문질렀다…
;--------------------------------------------------------
;3P
;--------------------------------------------------------
ELSEIF SELECTCOM == 64
	IF ASSIPLAY
		IF TFLAG:40 == 1
			PRINTFORM %플레이어는()% %CALLNAME:TARGET%의 음순에 삽입하면서, 
		ELSEIF TFLAG:40 == 2
			PRINTFORM %플레이어는()% %CALLNAME:TARGET%의 애널에 삽입하면서, 
		ELSEIF TFLAG:40 == 3
			PRINTFORM %플레이어는()% %CALLNAME:TARGET%에게 삼키게하면서, 
		ENDIF
		IF TFLAG:41 == 1
			PRINTFORML %CALLNAME:ASSI%에게 음순을 관철하게했다…
		ELSEIF TFLAG:41 == 2
			PRINTFORML %CALLNAME:ASSI%에게 애널에 삽입하게했다…
		ELSEIF TFLAG:41 == 3
			PRINTFORML %CALLNAME:ASSI%에게 입안을 유린하게했다…
		ENDIF
	ELSE
		IF TFLAG:41 == 1
			PRINTFORM %CALLNAME:ASSI%에게 %CALLNAME:TARGET%의 음순을 관철하게하면서, 
		ELSEIF TFLAG:41 == 2
			PRINTFORM %CALLNAME:ASSI%에게 %CALLNAME:TARGET%의 애널에 삽입하게하면서, 
		ELSEIF TFLAG:41 == 3
			PRINTFORM %CALLNAME:ASSI%에게 %CALLNAME:TARGET%의 입 안을 범하게하면서, 
		ENDIF
		IF TFLAG:40 == 1
			PRINTFORML %플레이어는()% 음순에 삽입했다…
		ELSEIF TFLAG:40 == 2
			PRINTFORML %플레이어는()% 애널에 삽입했다…
		ELSEIF TFLAG:40 == 3
			PRINTFORML %플레이어는()% 입을 유린했다…
		ENDIF
	ENDIF
;--------------------------------------------------------
;조수를 범하게 한다
;--------------------------------------------------------
ELSEIF SELECTCOM == 65
	PRINTFORM %CALLNAME:TARGET%에게 %플레이어를()% 
	IF TALENT:PLAYER:85 == 1
		PRINTFORM 안겨
	ELSE
		PRINTFORM 범하게 해
	ENDIF
	PRINTFORML 주기로 했다…
	PRINTFORM %타겟은()% 

	IF ABL:20 <= 2
		PRINTFORM 당황해 하면서 
	ELSEIF ABL:20 <= 4
		PRINTFORM 반응을 즐기듯이
	ELSE
		PRINTFORM 가학적인 미소를 지으며 
	ENDIF
	PRINTFORM %플레이어를()% 뒤에서 
	IF TALENT:121 || TALENT:121
		SIF TALENT:121
			PRINTFORM 후타나리
		PRINTFORM 페니스
	ELSE
		SIF TALENT:83
			PRINTFORM 극태
		PRINTFORM 페니스밴드
	ENDIF
	PRINTFORML 로 찔렀다…
;--------------------------------------------------------
;두개 펠라
;--------------------------------------------------------
ELSEIF SELECTCOM == 66
	PRINTFORML %CALLNAME:TARGET%에게 %플레이어와()% %CALLNAME:PLAYER%의 페니스에 동시에 봉사하게했다…
;--------------------------------------------------------
;더블 펠라
;--------------------------------------------------------
ELSEIF SELECTCOM == 68
	PRINTFORML %타겟과()% %조수는()% 둘이 함께 %CALLNAME:MASTER%의 페니스를 혀로 핥고 있다…
;--------------------------------------------------------
;식스나인
;--------------------------------------------------------
ELSEIF SELECTCOM == 69
	PRINTFORM %타겟과()% 
	IF TEQUIP:89
		PRINTFORM 개는 
	ELSEIF ASSI > 0 && ASSIPLAY
		PRINTFORM %조수는()% 
	ENDIF
	PRINTL 서로의 성기를 빨았다…
;--------------------------------------------------------
;더블 스마타
;--------------------------------------------------------
ELSEIF SELECTCOM == 70
	PRINTFORML %타겟과()% %조수는()% 자신들의 성기 사이에 %CALLNAME:MASTER%의 페니스를 끼워, 둘이 함께 문질렀다…
;--------------------------------------------------------
;더블 파이즈리
;--------------------------------------------------------
ELSEIF SELECTCOM == 71
	PRINTFORML %타겟과()% %조수는()% 두 명의 유방으로 %CALLNAME:MASTER%의 페니스를 양쪽에서 끼우고, 문지르고 있다…
;--------------------------------------------------------
;이라마치오
;--------------------------------------------------------
ELSEIF SELECTCOM == 80
	PRINTFORML %플레이어는()% %CALLNAME:TARGET%의 목 안쪽까지 난폭하게 찔러넣었다…
;--------------------------------------------------------
;피스트퍽
;--------------------------------------------------------
ELSEIF SELECTCOM == 81
	PRINTFORML %CALLNAME:TARGET%의 질은 %CALLNAME:PLAYER%의 주먹으로 휘저어졌다.
	IF TFLAG:899 == 0
		IF ABL:2 == 2
			PRINTFORML %CALLNAME:TARGET%의 질은 뻥하니 입을 벌리고 있다
		ELSEIF ABL:2 == 3
			PRINTFORML %CALLNAME:TARGET%의 질은 벌려진채로 좀처럼 돌아오지 않는다
		ELSEIF ABL:2 == 4
			PRINTFORML %CALLNAME:TARGET%의 입을 벌린 질구 안쪽에 자궁구가 보이고 있다
		ELSEIF ABL:2 >= 5
			PRINTFORML %타겟은()%, 자신의 질구를 손가락으로 벌리고, 황홀해하고 있다.
		ENDIF
	ENDIF
;--------------------------------------------------------
;애널피스트
;--------------------------------------------------------
ELSEIF SELECTCOM == 82
	PRINTFORML %CALLNAME:TARGET%의 항문은 %CALLNAME:PLAYER%의 주먹으로 휘저어졌다.
	IF TFLAG:899 == 0
		IF ABL:3 == 2
			PRINTFORML %CALLNAME:TARGET%의 항문은 뻥하니 입을 벌리고 있다
		ELSEIF ABL:3 == 3
			PRINTFORML %CALLNAME:TARGET%의 항문은 벌려진채로 좀처럼 돌아오지 않는다
		ELSEIF ABL:3 == 4
			PRINTFORML %CALLNAME:TARGET%의 항문이 입을 벌려 장내가 보이고 있다.
		ELSEIF ABL:3 >= 5
			PRINTFORML %타겟은()%, 자신의 항문을 손가락으로 벌리고, 황홀해하고 있다.
		ENDIF
	ENDIF
;--------------------------------------------------------
;양구멍피스트
;--------------------------------------------------------
ELSEIF SELECTCOM == 83
	PRINTFORML %CALLNAME:TARGET%의 질과 항문은 %CALLNAME:PLAYER%의 주먹으로 휘저어졌다.
	IF TFLAG:899 == 0
		IF ABL:2 <= 4 || ABL:3 <= 4
			PRINTFORML %타겟은()% 양 다리를 힘없이 벌리고 있다.
			PRINTFORML 한껏 벌려진 질구와 항문은 점액과 대소변으로 더럽혀져있다.
		ELSEIF ABL:2 >= 5 && ABL:3 >= 5
			PRINTFORML %타겟은()% 여운을 맛보고 있다.
			PRINTFORML 양손의 손가락이, 완전히 벌려져 안쪽을 전부 드러내고있는, 질구와 항문 주위를 만지고 있다.
		ENDIF
	ENDIF
;--------------------------------------------------------
;방뇨
;--------------------------------------------------------
ELSEIF SELECTCOM == 85
						;****슬라임パッチ追加ここから****
	;슬라임プレイ中に방뇨
	IF TEQUIP:150
		PRINTFORML %CALLNAME:TARGET%의 오줌이 슬라임에게 흡수되고 있다…
						;****슬라임パッチ追加ここまで****
	;촉수プレイ中に방뇨
	ELSEIF TEQUIP:90
		PRINTFORML 촉수에게 휘감겨진 %타겟은()%, 오줌을 공중에 뿌려댔다…
	;밧줄사용중に방뇨
	ELSEIF TEQUIP:44
		PRINTFORML 단단히 묶인 %타겟은()% 움직이지 못하는채로 오줌을 싸기 시작했다
		SIF CFLAG:42 != 69 || (CFLAG:40 & 64) == 0
			PRINTFORML 따끈따끈한 김이 나는 오줌이 %CALLNAME:TARGET%의 위로 역류해, 몸을 더럽혀간다…
	;顔面기승위プレイ中に방뇨
	ELSEIF TEQUIP:55
		PRINTFORM %타겟은()% 
			;IF ASSI > 0
			;	PRINTFORML %CALLNAME:ASSI%의 입 안에 방뇨를 시작했다
			;ELSE
				PRINTFORML %CALLNAME:PLAYER%의 입 안에 오줌을 쌌다
			;ENDIF
	;기저귀の中に방뇨
	ELSEIF CFLAG:42 == 69 && (CFLAG:40 & 64) && ABL:10 >= 3 && ABL:17 >= 3
		PRINTFORML %플레이어가()% 명령하자, %타겟은()% 그 자리에서 기저귀 안에 오줌을 쌌다
		PRINTFORML %타겟은()% 황홀한 얼굴로 몸을 떨고 있다…
	ELSEIF CFLAG:42 == 69 && (CFLAG:40 & 64)
		PRINTFORML %CALLNAME:PLAYER%에게 명령받아, %타겟은()% 얼굴을 새빨갛게 물들이고 기저귀 안에 오줌을 쌌다…
	;즈코 인형の中で방뇨
	ELSEIF CFLAG:42 == 11 && (CFLAG:40 & 64) && ABL:10 >= 3 && ABL:17 >= 3
		PRINTFORM %플레이어가()% 명령하자, %타겟은()% 기쁨에 차 
		CALL PRINT_CLOTHTYPE_SPECIAL
		PRINTFORML  안에 오줌을 쌌다…
		PRINTFORML 새로운 종류의 쾌락을 발견하고 있는 것 같다
	ELSEIF CFLAG:42 == 11 && (CFLAG:40 & 64)
		PRINTFORM %타겟은()% 
		CALL PRINT_CLOTHTYPE_SPECIAL
		PRINTFORML  안에 오줌을 쌌다…
		PRINTFORML 바깥쪽에서는 잘 알 수 없지만, 아무래도 안에서 울고 있는 것 같다…
	;服を着たまま방뇨
	ELSEIF (CFLAG:40 & 8) && ABL:10 >= 3 && ABL:17 >= 3 && TALENT:28 && TEQUIP:44 == 0
		PRINTFORM %타겟은()% 어떤 망설임도 없이, 기세 좋게 
		CALL PRINT_CLOTHTYPE_MAIN2
		PRINTFORML  안에 오줌을 내뿜기 시작했다…
		PRINTFORM 이 쪽과 시선이 마주쳤다고 생각하면, 갑작스레 
		CALL PRINT_CLOTHTYPE_MAIN2
		PRINTFORML %조사만처리(RESULT+2,"를")% 들어올리고, 
		IF (CFLAG:40 & 1)
			PRINT 오줌으로 질척해진 팬티
		ELSE
			PRINT 오줌을 계속 싸고있는 성기
		ENDIF
		PRINTFORML 를 보이면서 V자를 해보였다
		PRINTFORML 뭘 생각하고 있는건지…
	ELSEIF ((CFLAG:40 & 8) || (CFLAG:40 & 16)) && ABL:10 >= 3 && ABL:17 >= 3
		PRINTFORM %타겟은()% 어떤 망설임도 없이, 기세 좋게 
		CALL PRINT_CLOTHTYPE_MAIN2
		PRINTFORML %조사만처리(RESULT+2,"를")% 입은채로 오줌을 싸기 시작했다…
	ELSEIF (CFLAG:40 & 8) || (CFLAG:40 & 16)
		PRINTFORM %타겟은()% 고개 숙인채로, 입을 다물고 
		CALL PRINT_CLOTHTYPE_MAIN2
		PRINTFORML  안에 방뇨했다…
	;パンツの中に방뇨
	ELSEIF (CFLAG:40 & 1) && ABL:10 >= 3 && ABL:17 >= 3
		PRINTFORML %타겟은()% 황홀한 얼굴로 팬티 안에 오줌을 싸기 시작했다…
		PRINTFORML 질척질척하게 젖은 팬티의 감촉에 중독되어가고 있는 것 같다…
	ELSEIF (CFLAG:40 & 1)
		PRINTFORML %타겟은()% 새빨간 얼굴로 팬티 안에 방뇨했다…
	ELSEIF TALENT:57 && ABL:10 >= 3 && ABL:21 >= 3 && TALENT:132
		PRINTFORM %플레이어가()% %CALLNAME:TARGET%의 양 다리를 안아올리고, 오줌구멍을 손 끝으로 간지럽히자, 
		PRINTFORML %타겟은()% 작은 몸을 떨며, 반사적으로 오줌을 내뿜어버렸다…
	ELSEIF TALENT:57 && ABL:10 >= 3 && ABL:21 >= 3
		PRINTFORM %플레이어가()% %CALLNAME:TARGET%의 요도구를 가볍게 손끝으로 자극하자, 
		PRINTFORML %타겟은()% 참지 못하고, 반사적으로 오줌을 내뿜어버렸다…
	ELSEIF ABL:10 >= 3 && TALENT:28 && ABL:17 >= 3 && TEQUIP:53
		PRINTFORM %타겟이()% 뒤에서 손으로 다리를 M자로 활짝 벌리자, 
		PRINTFORML 비디오카메라를 향해 피스 싸인을 해보이며 방뇨했다…
	;복종が3以上, 노출벽が3以上の時の야외 플레이중
	ELSEIF ABL:10 >= 3 && ABL:17 >= 3 && TEQUIP:54 == 1
		PRINTFORML %타겟은()% %CALLNAME:PLAYER%의 명령대로, 
		PRINTFORML 왕래가 많은 길 한 가운데에서 다리를 벌려 성기를 노출하고, 	
		PRINTFORML 통행인의 시선도 신경쓰지 않고, 선채로 오줌을 싸기 시작했다…
	ELSEIF ABL:10 >= 3 && ABL:17 >= 3
		PRINTFORM %타겟은()% 
		SIF TEQUIP:44 == 0
			PRINTFORM 뒤에서 손으로 다리를 M자로 활짝 벌리고, 
		PRINTFORML %CALLNAME:PLAYER%에게 보이도록 방뇨했다…
	ELSEIF TEQUIP:54 == 1
		PRINTFORML %타겟은()% 수치와 굴욕 투성이가 되어, 
		SIF TALENT:45 == 0 && TALENT:310 == 0
			PRINTFORM 눈물을 띄운채로, 
		PRINTFORML 풀 숲 안에서 방뇨했다…
	ELSEIF TEQUIP:57
		PRINTFORML %타겟은()% 거울 속에 비치는 자신을 보지 않게하면서 주저앉아 오줌을 싸기 시작했다…
	ELSEIF TALENT:132
		PRINTFORML %타겟은()% %CALLNAME:PLAYER%에게 어린아이처럼 양 다리를 안아올려진채로 오줌을 싸기 시작했다…
	ELSE
		PRINTFORML %타겟은()% 수치심에 뺨을 붉히면서, 그 자리에 주저앉아 오줌을 싸기 시작했다…
	ENDIF
;--------------------------------------------------------
;피어싱
;--------------------------------------------------------
ELSEIF SELECTCOM == 87
	PRINTFORM %플레이어는()% %CALLNAME:TARGET%의 
	IF P == 1
		IF PALAM:5 >= PALAMLV:4
			PRINT 발기한 
		ELSEIF ABL:1 >= 4
			PRINT 민감한 
		ELSEIF TALENT:132
			PRINT 어린 
		ENDIF
		PRINT 양 유두
	ELSEIF P == 2
		PRINT 배꼽
	ELSEIF P == 4
		IF PALAM:5 >= PALAMLV:4
			PRINT 음란하게 떨리는 
		ELSEIF ABL:2 >= 4
			PRINT 민감한 
		ELSEIF TALENT:132
			PRINT 어린 
		ENDIF
		PRINT 좌우의 소음순
	ELSEIF P == 8
		IF PALAM:5 >= PALAMLV:4
			PRINT 발기한 
		ELSEIF ABL:0 >= 4
			PRINT 민감한 
		ELSEIF TALENT:132
			PRINT 작은 
		ENDIF
		IF TALENT:121 || TALENT:122
			PRINT 페니스
		ELSE
			PRINT 클리토리스
		ENDIF
	ELSEIF P == 16
		PRINT 혀 끝
	ENDIF
	IF CFLAG:7 & P
		PRINTFORMW 에서 피어스를 빼냈다…
	ELSE
		PRINTFORMW 에 피어싱했다…
	ENDIF
;--------------------------------------------------------
;제모
;--------------------------------------------------------
ELSEIF SELECTCOM == 88
	IF TEQUIP:44
		PRINTFORM 다리를 벌리고 성기를 내밀어 %CALLNAME:TARGET%의 
	ELSEIF ABL:10 >= 3 && ABL:17 >= 4
		PRINTFORM 스스로 조르듯이 다리를 벌리고 성기를 내밀어 %CALLNAME:TARGET%의 
	ELSE
		PRINTFORM 다리를 벌리게 한 %CALLNAME:TARGET%의 
	ENDIF
	SIF CFLAG:6 > 12
		PRINT 우거진 
	PRINTFORML 음모를, %플레이어는()% 깨끗하게 깎아냈다…
;--------------------------------------------------------
;수간 플레이
;--------------------------------------------------------
ELSEIF SELECTCOM == 89
	IF CFLAG:42 == 11 && (CFLAG:40 & 64)
		CALL PRINT_CLOTHTYPE_SPECIAL
	ELSE
		PRINTFORM %CALLNAME:TARGET%
	ENDIF
	SIF (CFLAG:42 != 11 || (CFLAG:40 & 64) == 0) && TEQUIP:89
		PRINT 의 몸
	IF TEQUIP:89
		PRINTFORML 에게서 개를 떼어놓았다…
	ELSE
		PRINTFORML 에게 개를 밀어붙였다…
	ENDIF
					;追加ここから
;-------------------------------------------------
;가슴구멍
;-------------------------------------------------
ELSEIF SELECTCOM == 143
	PRINTFORML %플레이어는()% %조사처리(CALLNAME,"를")% 붙잡고 
	SIF TEQUIP:44 == 1
	PRINTFORM 꽉 묶여 조이는 
	;거유
	IF TALENT:110
		PRINT 풍만한 유방
	;폭유
	ELSEIF TALENT:114
		PRINT 너무 풍만한 유방
	ENDIF
	PRINTFORML 의 틈새에 페니스를 집어넣고
	PRINTFORML 부드럽고 따뜻한 가슴에 빨려들어가듯 허리를 흔들었다……
					;追加ここまで
						;****追加ここから****
						;****追加ここから****
;--------------------------------------------------------
;로터자위
;--------------------------------------------------------
ELSEIF SELECTCOM == 90
	
	IF TALENT:122
		SAVESTR:2 = 음경을 문지르고
		SAVESTR:3 = 수컷
	ELSEIF TALENT:121 == 0
		SAVESTR:2 = 비부를 만지작거리고
		SAVESTR:3 = 암컷
	ELSEIF TALENT:121 == 1
		SAVESTR:2 = 발기한 후타나리 페니스를 문지르며 비부를 만지작거리고
		SAVESTR:3 = 암컷
	ENDIF
	
	IF A < V
		PRINTFORML %CALLNAME:TARGET%에게 자위를 명령해보았지만 거부당했다. 좀 더 지도할 필요가 있다.
	ELSEIF A > V && A < 50
		PRINTFORM %플레이어가()% 자위를 명령하자, %타겟은()% 
		;着衣設定
		IF CFLAG:40 & 8
			CALL PRINT_CLOTHTYPE_MAIN2
			PRINT 의 앞을 올려 성기를 드러내고 
		ENDIF
		SIF TALENT:15
			PRINT 자존심은 굽히지 않고 
		IF TALENT:22
			PRINT 무표정한 얼굴로 묵묵히 
		ELSEIF TALENT:10 || TALENT:14
			PRINT 떨리는 손 끝으로 
		ELSEIF TALENT:11
			PRINT 반항적으로 노려봤지만 
		ELSEIF TALENT:12
			PRINT 굴욕으로 입술을 깨물고 
		ELSEIF TALENT:35
			PRINT 수치심에 귀까지 완전히 빨개져 
		ENDIF
		SIF TEQUIP:53
			PRINT 비디오카메라 앞에서 
		SIF PALAM:5 >= PALAMLV:3
			PRINT 발기한 
		PRINTL 클리토리스에 진동하는 로터를 대고 자위를 시작했다.
	ELSEIF A >=50 && A < 80
		PRINTFORML 지도의 성과인지, %타겟은()% %CALLNAME:PLAYER%의 명령에 복종해 자위를 시작했다.
	ELSEIF A >= 80
		PRINTFORML %플레이어가()% 명령하자 기뻐하며, %타겟은()% 완전히 사육된 %SAVESTR:3%의 표정으로 자위를 시작했다.
	ENDIF
	IF TEQUIP:11 && TEQUIP:13 && ABL:2 >= 5 && ABL:3 >= 5 && ABL:0 >= 5 && ABL:1 >= 5 && PALAM:5 >= PALAMLV:4 && PREVCOM == 3
		PRINTFORML %타겟은()% 침을 흘리며, 움찔움찔 몸을 떨면서 울고 있다
		PRINTFORML 절정이 멈추지 않아 계속해서 가고 있는 것 같다…
	ELSEIF TEQUIP:11 && ABL:2 >= 5 && ABL:0 >= 5 && ABL:1 >= 5 && PALAM:5 >= PALAMLV:4 && PREVCOM == 3
		PRINTFORML %타겟은()% 애액이 흩날리는 것도 신경쓰지 않고, 바이브를 격렬하게 움직이고 있다.
		PRINTFORML 이제는 다른 일을 신경쓸 여유도 없는 것 같다…
	ELSEIF TEQUIP:13 && ABL:3 >= 5 && ABL:0 >= 5 && ABL:1 >= 5 && PALAM:5 >= PALAMLV:4 && PREVCOM == 3
		PRINTFORML %타겟은()% 부들부들 몸을 떨면서, 아누스에 바이브를 삽입하고 있다
		PRINTFORML 주름을 밀고 들어올때마다 몇번이나 절정에 도달해버리는 것 같다…
	ELSEIF ABL:10 >= 3 && ABL:0 >= 5 && ABL:1 >= 5 && PALAM:5 >= PALAMLV:4 && PREVCOM == 3
		PRINTFORML %타겟은()% 교성을 흘리며 일심분란하게 자신의 %SAVESTR:2% 있다
		PRINTFORML 멈추고 싶어도 손이 멋대로 움직여 멈춰지지 않는 것 같다…
	ENDIF

	SAVESTR:2 = %STR:0%
	SAVESTR:3 = %STR:0%

;--------------------------------------------------------
;전동마사지기자위
;--------------------------------------------------------
ELSEIF SELECTCOM == 91
	
	IF TALENT:122
		SAVESTR:2 = 음경을 문지르고
		SAVESTR:3 = 수컷
	ELSEIF TALENT:121 == 0
		SAVESTR:2 = 비부를 만지작거리고
		SAVESTR:3 = 암컷
	ELSEIF TALENT:121 == 1
		SAVESTR:2 = 발기한 후타나리 페니스를 문지르며 비부를 만지작거리고
		SAVESTR:3 = 암컷
	ENDIF
	
	IF A < V
		PRINTFORML %CALLNAME:TARGET%에게 자위를 명령해보았지만 거부당했다. 좀 더 지도할 필요가 있다.
	ELSEIF A > V && A < 50
		PRINTFORM %플레이어가()% 자위를 명령하자, %타겟은()% 
		;着衣設定
		IF CFLAG:40 & 8
			CALL PRINT_CLOTHTYPE_MAIN2
			PRINT 의 앞을 올려 성기를 드러내고 
		ENDIF
		SIF TALENT:15
			PRINT 자존심은 굽히지 않고 
		IF TALENT:22
			PRINT 무표정한 얼굴로 묵묵히 
		ELSEIF TALENT:10 || TALENT:14
			PRINT 떨리는 손 끝으로 
		ELSEIF TALENT:11
			PRINT 반항적으로 노려봤지만 
		ELSEIF TALENT:12
			PRINT 굴욕으로 입술을 깨물고 
		ELSEIF TALENT:35
			PRINT 수치심에 귀까지 완전히 빨개져 
		ENDIF
		SIF TEQUIP:53
			PRINT 비디오카메라 앞에서 
		IF TALENT:121 || TALENT:122
			PRINT 고간
		ELSE
			PRINT 비소
		ENDIF
		PRINTL 에 진동하는 전동마사지기를 대고 자위를 시작했다.
	ELSEIF A >=50 && A < 80
		PRINTFORML 지도의 성과인지, %타겟은()% %CALLNAME:PLAYER%의 명령에 복종해 자위를 시작했다.
	ELSEIF A >= 80
		PRINTFORML %플레이어가()% 명령하자 기뻐하며, %타겟은()% 완전히 사육된 %SAVESTR:3%의 표정으로 자위를 시작했다.
	ENDIF
	IF TEQUIP:11 && TEQUIP:13 && ABL:2 >= 5 && ABL:3 >= 5 && ABL:0 >= 5 && ABL:1 >= 5 && PALAM:5 >= PALAMLV:4 && PREVCOM == 3
		PRINTFORML %타겟은()% 침을 흘리며, 움찔움찔 몸을 떨면서 울고 있다
		PRINTFORML 절정이 멈추지 않아 계속해서 가고 있는 것 같다…
	ELSEIF TEQUIP:11 && ABL:2 >= 5 && ABL:0 >= 5 && ABL:1 >= 5 && PALAM:5 >= PALAMLV:4 && PREVCOM == 3
		PRINTFORML %타겟은()% 애액이 흩날리는 것도 신경쓰지 않고, 바이브를 격렬하게 움직이고 있다.
		PRINTFORML 이제는 다른 일을 신경쓸 여유도 없는 것 같다…
	ELSEIF TEQUIP:13 && ABL:3 >= 5 && ABL:0 >= 5 && ABL:1 >= 5 && PALAM:5 >= PALAMLV:4 && PREVCOM == 3
		PRINTFORML %타겟은()% 부들부들 몸을 떨면서, 아누스에 바이브를 삽입하고 있다
		PRINTFORML 주름을 밀고 들어올때마다 몇번이나 절정에 도달해버리는 것 같다…
	ELSEIF ABL:10 >= 3 && ABL:0 >= 5 && ABL:1 >= 5 && PALAM:5 >= PALAMLV:4 && PREVCOM == 3
		PRINTFORML %타겟은()% 교성을 흘리며 일심분란하게 자신의 %SAVESTR:2% 있다
		PRINTFORML 멈추고 싶어도 손이 멋대로 움직여 멈춰지지 않는 것 같다…
	ENDIF

	SAVESTR:2 = %STR:0%
	SAVESTR:3 = %STR:0%

;--------------------------------------------------------
;유두로터
;--------------------------------------------------------
ELSEIF SELECTCOM == 92
		IF (CFLAG:40 & 64) && CFLAG:42 <= 50
		CALL PRINT_CLOTHTYPE_SPECIAL
		PRINT  너머로, 
	ELSEIF CFLAG:40 & 28
		CALL PRINT_CLOTHTYPE_MAIN2
		PRINT  너머로, 
	ELSEIF (CFLAG:40 & 2)
		PRINT 속옷 너머로, 
		ENDIF
		PRINTFORM %CALLNAME:TARGET%의 
			IF TALENT:116
				PRINT 완전히 평평한 
			ELSEIF TALENT:100 && TALENT:109
				PRINT 간신히 부푼 것을 알 수 있을 정도의 
			ELSEIF TALENT:109
				PRINT 표준보다 약간 작은 
			ELSEIF TALENT:110 && TEQUIP:89 == 0
				PRINT 손에서 넘쳐흐를 것 같은 
			ELSEIF TALENT:114
				PRINT 한 쪽 가슴만으로도 넘쳐흐를 것 같은 
			ENDIF
		PRINTL 가슴에 진동하는 로터를 꽉 눌렀다…
						;****追加ここまで****
;--------------------------------------------------------
;촉수생물
;--------------------------------------------------------
ELSEIF SELECTCOM == 100
	IF TEQUIP:90
		PRINTFORML %CALLNAME:TARGET%에게서 때어내 우리에 넣었다…
	ELSE
		PRINTFORM %플레이어가()% 조종하는 이형의 촉수가, 
		IF CFLAG:42 == 11 && (CFLAG:40 & 64)
			CALL PRINT_CLOTHTYPE_SPECIAL
		ELSE
			PRINTFORM %CALLNAME:TARGET%의 몸
		ENDIF
		PRINTFORML 에 달라붙었다…
	ENDIF
;--------------------------------------------------------
;촉수 삽입
;--------------------------------------------------------
ELSEIF SELECTCOM == 101
	IF TEQUIP:11
		PRINTFORML 촉수가 %CALLNAME:TARGET%의 성기에서 뽑아내졌다
	ELSE
		IF CFLAG:42 == 11 && (CFLAG:40 & 64)
			PRINTFORM 촉수 무리는 
			CALL PRINT_CLOTHTYPE_SPECIAL
			PRINTFORM 의 틈새를 파고들어, 
		ENDIF
		PRINTFORML 세세한 돌기로 덮인 촉수가 %CALLNAME:TARGET%의 성기를 파고 들었다
	ENDIF
;--------------------------------------------------------
;애널촉수
;--------------------------------------------------------
ELSEIF SELECTCOM == 102
	IF TEQUIP:13
		PRINTFORML 촉수는 %CALLNAME:TARGET%의 아누스에서 뽑아내졌다
	ELSE
		IF CFLAG:42 == 11 && (CFLAG:40 & 64)
			PRINTFORM 촉수 무리는 
			CALL PRINT_CLOTHTYPE_SPECIAL
			PRINTFORM 의 틈새를 파고들어, 
		ENDIF
		PRINTFORML 장대한 촉수가 %CALLNAME:TARGET%의 애널에 파고들었다
	ENDIF
;--------------------------------------------------------
;촉수 클리자극
;--------------------------------------------------------
ELSEIF SELECTCOM == 103
	IF TEQUIP:14
		PRINTFORML 촉수는 %CALLNAME:TARGET%의 클리 고문을 중단했다
	ELSE
		IF CFLAG:42 == 11 && (CFLAG:40 & 64)
			PRINTFORM 촉수 무리는 
			CALL PRINT_CLOTHTYPE_SPECIAL
			PRINTFORM 의 틈새를 파고들어, 
		ENDIF
		PRINTFORML 세세하게 뻗은 촉수가 %CALLNAME:TARGET%의 클리토리스를 괴롭히기 시작했다
	ENDIF
;--------------------------------------------------------
;촉수 유두자극
;--------------------------------------------------------
ELSEIF SELECTCOM == 104
	IF TEQUIP:15
		PRINTFORML 촉수는 %CALLNAME:TARGET%의 유두에서 떨어졌다
	ELSE
		IF CFLAG:42 == 11 && (CFLAG:40 & 64)
			PRINTFORM 촉수 무리는 
			CALL PRINT_CLOTHTYPE_SPECIAL
			PRINTFORM 의 틈새를 파고들어, 
		ENDIF
		PRINTFORML 세세하게 꿈틀거리는 촉수가 %CALLNAME:TARGET%의 유두를 만지작거리기 시작했다
	ENDIF
;--------------------------------------------------------
;촉수 착유
;--------------------------------------------------------
ELSEIF SELECTCOM == 105
	IF TEQUIP:16
		PRINTFORML 촉수는 %CALLNAME:TARGET%의 유두를 해방했다
	ELSE
		IF CFLAG:42 == 11 && (CFLAG:40 & 64)
			PRINTFORM 촉수 무리는 
			CALL PRINT_CLOTHTYPE_SPECIAL
			PRINTFORM 의 틈새를 파고들어, 
		ENDIF
		PRINTFORML 흡착성있는 촉수가 %CALLNAME:TARGET%의 유두를 빨아들이기 시작했다
	ENDIF
;--------------------------------------------------------
;촉수 긴박
;--------------------------------------------------------
ELSEIF SELECTCOM == 106
	IF TEQUIP:44
		PRINTFORML 촉수는 %CALLNAME:TARGET%의 구속을 풀었다
	ELSE
		IF CFLAG:42 == 11 && (CFLAG:40 & 64)
			PRINTFORM 촉수 무리는 
			CALL PRINT_CLOTHTYPE_SPECIAL
			PRINTFORM 의 틈새를 파고들어, 
		ENDIF
		PRINTFORML 무수한 촉수가 %CALLNAME:TARGET%의 몸을 묶었다
	ENDIF
;--------------------------------------------------------
;촉수 관장
;--------------------------------------------------------
ELSEIF SELECTCOM == 107
	IF TEQUIP:46
		PRINTFORML 촉수가 빼내어지자 %타겟은()% 오물을 항문에서 흩뿌렸다
	ELSE
		IF CFLAG:42 == 11 && (CFLAG:40 & 64)
			PRINTFORM 촉수 무리는 
			CALL PRINT_CLOTHTYPE_SPECIAL
			PRINTFORM 의 틈새를 파고들어, 
		ENDIF
		PRINTFORML 극태의 촉수가 %CALLNAME:TARGET%의 국혈을 막아, 체액을 흘려넣었다
	ENDIF
;--------------------------------------------------------
;촉수 구욕
;--------------------------------------------------------
ELSEIF SELECTCOM == 108
	IF TEQUIP:98
		PRINTFORML 촉수는 %CALLNAME:TARGET%의 입에서 뽑아졌다
	ELSE
		IF CFLAG:42 == 11 && (CFLAG:40 & 64)
			PRINTFORM 촉수 무리는 
			CALL PRINT_CLOTHTYPE_SPECIAL
			PRINTFORM 의 틈새를 파고들어, 
		ENDIF
		PRINTFORML 젖어서 빛나는 촉수가 %CALLNAME:TARGET%의 입안을 파고들었다
	ENDIF
;--------------------------------------------------------
;촉수 페니스자극
;--------------------------------------------------------
ELSEIF SELECTCOM == 109
	IF TEQUIP:17
		PRINTFORML 촉수는 %CALLNAME:TARGET%의 페니스를 해방했다
	ELSE
		IF CFLAG:42 == 11 && (CFLAG:40 & 64)
			PRINTFORM 촉수 무리는 
			CALL PRINT_CLOTHTYPE_SPECIAL
			PRINTFORM 의 틈새를 파고들어, 
		ENDIF
		PRINTFORML 음란하게 진동하고 있는 촉수 무리가 %CALLNAME:TARGET%의 페니스를 휘어감고, 일제히 문질러대기 시작했다
	ENDIF
;--------------------------------------------------
;삽입G스팟자극
;--------------------------------------------------
ELSEIF SELECTCOM == 120
	;승마G스팟고문
	IF PREVCOM == 34 && TFLAG:899 == 0
		PRINTFORMW %타겟은()% 상체를 크게 움직여, 질내의 천장에 페니스를 찔러댔다…
		IF EXP:0 <= 30
			PRINTFORMW %타겟은()% 당황하면서도 행위를 계속하고 있다…
		ELSEIF EXP:0 <= 50
			PRINTFORMW %타겟은()% 쾌감을 쫓아 허리를 계속해서 흔들고 있다…
		ELSEIF EXP:0 <= 80
			PRINTFORMW %타겟은()% 엉덩이를 움츠리고, 조금씩 허리를 흔들며 교성을 지르고 있다…
		ELSEIF EXP:0 <= 120
			PRINTFORML %타겟은()% 뒤쪽에 손을 대고, 허리를 부들부들 떨며 쾌락을 탐내고 있다…
			PRINTFORMW 페니스 끝이, 날카롭게 G스팟을 찌르고 있다…
		ELSE
			PRINTFORML %타겟은()% 여자의 급소를, 스스로의 허리 움직임으로 마음껏 자극하고 있다…
			PRINTFORMW 무거운 물소리가 들릴때마다, 콧소리와함께 단정치 못한 소리가 새어나오고 있다…
		ENDIF
	;삽입G스팟자극
	ELSE
		;정상위
		IF PREVCOM == 20
			PRINTFORMW 허리를 움직여, %CALLNAME:TARGET%의 질내의 천장을 조금씩 문질러주었다…
		;후배위
		ELSEIF PREVCOM == 21
			PRINTFORMW 상체를 구부려, %CALLNAME:TARGET%의 천장을 파내듯이 찔러넣었다…
		;대면좌위
		ELSEIF PREVCOM == 22
			PRINTFORMW 상체를 떼어놓고, %CALLNAME:TARGET%의 아랫배를 눌러 질내의 천장을 문질러주었다…
		;배면좌위
		ELSEIF PREVCOM == 23
			PRINTFORMW %타겟을()% 들어올려, 흔들듯이 질내의 천장을 집요하게 자극했다…
		;乱れ牡丹
		;ELSEIF PREVCOM == 612
			;PRINTFORML %플레이어는()% 거울 앞에서 %CALLNAME:PLAYER%의 치태를 과시해
			;PRINTFORMW %타겟을()%持ち上げ, 揺するように膣内の天井を執拗に刺激した…
		ENDIF
		IF TFLAG:899 == 0
			IF EXP:0 <= 30
				PRINTFORMW %타겟은()% 조심조심 허리를 흔들고 있다…
			ELSEIF EXP:0 <= 50
				PRINTFORMW %타겟은()% 입을 다물고 서투른 쾌락에 견디고 있다…
			ELSEIF EXP:0 <= 80
				PRINTFORMW %타겟은()% 약점을 괴롭혀져, 지나친 쾌감에 눈을 떨고 있다…
			ELSEIF EXP:0 <= 120
				PRINTFORML %플레이어는()% 허리에 힘을 써, 질내에서 부들부들 떨게 했다…
				PRINTFORMW %타겟은()% 과민한 천장을 문질러지는 감촉에 뜨거운 숨을 흘리고 있다…
			ELSE
				PRINTFORML %타겟은()% 넘쳐나는 침을 삼키며, 발가락을 만채로 머리를 흔들며 괴로워하고 있다…
				PRINTFORMW 제대로 호흡조차 하지 못하고, 온전한 소리도 되지 않는 허덕임을 반복한다…
			ENDIF
		ENDIF
	ENDIF
;--------------------------------------------------
;삽입자궁구자극
;--------------------------------------------------
ELSEIF SELECTCOM == 121
	;승마자궁구고문
	IF PREVCOM == 34 && TFLAG:899 == 0
		PRINTFORMW %타겟은()% %CALLNAME:PLAYER%의 가슴을 껴안고, 페니스를 뿌리까지 삼켰다…
		IF EXP:0 <= 30
			PRINTFORMW %타겟은()% 그대로 허리를 앞뒤로 흔들고 있다…
		ELSEIF EXP:0 <= 50 && TALENT:121
			PRINTFORMW %타겟은()% %CALLNAME:PLAYER%의 배에 페니스를 꽉 누르고 있다…
		ELSEIF EXP:0 <= 50
			PRINTFORMW %타겟은()% %CALLNAME:PLAYER%의 음모로 클리토리스를 자극하고 있는 것 같다…
		ELSEIF EXP:0 <= 80
			PRINTFORMW %타겟은()% 허리를 흔들며, 삽입감을 즐기며 뜨거운 숨을 토하고 있다…
		ELSEIF EXP:0 <= 120
			PRINTFORML %타겟은()% 쿵, 쿵하고 허리를 떨어트리며, 깊은 곳을 찌르는 감촉에 취하고 있다…
			PRINTFORMW 탄력있는 자궁구가 터져, 황홀함과 함께 아이를 임신할 준비를 시작하고 있다…
		ELSE
			PRINTFORML %타겟은()% 리드미컬하게 허리를 세로로 흔들며, 암컷 그 자체인 얼굴로 침을 흘리고 있다…
			PRINTFORMW 아이 만들기의 쾌락에 이성은 녹아버려, 암컷의 육체는 아이를 요구하며 탐욕으로 휘청이고 있다…
		ENDIF
	;삽입자궁구자극
	ELSE
		;정상위
		SIF PREVCOM == 20
			PRINTFORMW 허리를 붙잡고 깊게 찔러, %CALLNAME:TARGET%의 자궁구를 때렸다…			
		;후배위
		SIF PREVCOM == 21
			PRINTFORMW 엉덩이를 붙잡고 깊게 찔러, %CALLNAME:TARGET%의 자궁구를 때렸다…
		;대면좌위
		SIF PREVCOM == 22
			PRINTFORMW 허리를 깊게 안고, %CALLNAME:TARGET%의 자궁을 빙글빙글 괴롭히며 자극했다…
		;배면좌위
		SIF PREVCOM == 23
			PRINTFORMW 허리를 앞으로 꽉 누르듯이해서, %CALLNAME:TARGET%의 깊은 곳까지 찔러올렸다…
		;乱れ牡丹
		IF PREVCOM == 612
			PRINTFORML %플레이어는()% 거울 앞에서 %CALLNAME:PLAYER%의 치태를 보이듯이, 
			PRINTFORMW 허리를 앞으로 꽉 누르듯이해서, %CALLNAME:TARGET%의 깊은 곳까지 찔러올렸다…
		ENDIF
		IF EXP:0 <= 30 || (EXP:0 >= 31 && EXP:0 <= 50 && (PREVCOM == 21 || PREVCOM == 612))
			PRINTFORMW %타겟은()% 이물감이 강한것인지, 괴로워하고 있는 것 같다…
		ELSEIF EXP:0 <= 50 && TALENT:121
			PRINTFORMW %타겟은()% 음모와 배꼽을 페니스로 희롱당해, 콧소리를 흘리고 있다…
		ELSEIF EXP:0 <= 50
			PRINTFORMW %타겟은()% 음모에 클리토리스를 문질러져, 콧소리를 흘리고 있다…
		ELSEIF EXP:0 <= 80
			PRINTFORMW %타겟은()% 길고 달콤한 한숨을 내쉬며, 삽입감에 이를 꽉 물고 있다…
		ELSEIF EXP:0 <= 120
			PRINTFORML 의 자궁이 자식을 받아들일 준비를 마친 것 처럼, 끝 부분의 굴곡이 질 안쪽으로 안내한다…
			PRINTFORMW %CALLNAME:TARGET%의 자궁을 노크할 때마다, 입을 뻐끔뻐끔하며 격렬하게 떨고 있다…
		ELSE
			PRINTFORML 낮은 위치로 내려온 %CALLNAME:TARGET%의 자궁을 찌르며 흔들면, 
			PRINTFORMW %타겟은()% 미친듯이 괴로워하며, 암컷으로서 최고의 쾌락에 빠졌다…
		ENDIF
	ENDIF
;--------------------------------------------------------
;투구비비기
;--------------------------------------------------------
ELSEIF SELECTCOM == 122
	IF PALAM:3 > PALAMLV:3
		PRINTFORML %플레이어는()% %CALLNAME:TARGET%의 쿠퍼액으로 젖어 흔들리는 페니스와 당신의 페니스를 문질러댔다
	ELSE
		PRINTFORML %플레이어는()% %타겟과()% 딱딱하게 발기한 페니스를 서로 문질러댔다
	ENDIF
;--------------------------------------------------------
;파이즈리펠라
;--------------------------------------------------------
ELSEIF SELECTCOM == 123
	PRINTFORM %타겟은()% 
	SIF TALENT:85
		PRINT 황홀한 표정을 지으며 
	IF TALENT:109
		PRINTL 평평한 가슴에 페니스를 문지르며, 귀두를 혀로 핥았다…
	ELSEIF TALENT:110
		PRINTL 페니스를 풍만한 가슴으로 감싸고, 머리를 드러낸 페니스를 빨며 자극하고 있다…
	ELSEIF TALENT:114
		PRINTL 너무 풍만한 가슴으로 페니스를 감싸 숨겨버려, 자신의 가슴에 얼굴을 파묻고 페니스를 빨고 있다…
	ELSE
		PRINTL 가슴에 페니스를 끼우고, 페니스를 빨며 자극하고 있다…
	ENDIF
;--------------------------------------------------------
;딥스로트
;--------------------------------------------------------
ELSEIF SELECTCOM == 124
	PRINTFORM %타겟은()% 
	SIF TALENT:85
		PRINT 황홀한 표정을 지으며 
	SIF TALENT:52
		PRINT 혀로 막대를 휘어감고 
	PRINT 목 안쪽까지 페니스를 삼키고 
	IF TALENT:16 >= 3
		PRINTL 음탕한 소리를 내며 깊은 스트로크로 자극했다…
	ELSE
		PRINTL 있다…
	ENDIF
;--------------------------------------------------------
;펠라자위
;--------------------------------------------------------
ELSEIF SELECTCOM == 125
	PRINTFORM %타겟은()% 
	SIF TALENT:85
		PRINT 달아오른 표정으로 
	SIF TALENT:76
		PRINT 음탕한 표정으로 
	SIF TALENT:52
		PRINT 혀로 막대를 휘어감고 
	PRINT 페니스를 삼킨채로 자신의 
	IF TEQUIP:11 && TEQUIP:13
		PRINTL 비부와 애널에 삽입된 바이브를 움직였다…
	ELSEIF TEQUIP:11
		PRINTL 비부에 삽입된 바이브를 움직였다…
	ELSEIF TEQUIP:13
		PRINTL 애널에 삽입된 바이브를 움직였다…
	ELSE
		IF TALENT:122
			PRINTL 음경을 문질렀다…
		ELSE
			PRINTL 비부를 만지작거렸다…
		ENDIF
	ENDIF
;--------------------------------------------------------
;수음펠라
;--------------------------------------------------------
ELSEIF SELECTCOM == 126
	PRINTFORM %타겟은()% 
	SIF TALENT:85
		PRINT 황홀한 표정으로 
	SIF TALENT:52
		PRINT 혀로 막대를 휘어감고 
	IF TALENT:PLAYER:122
		PRINTL 페니스를 빨면서 다른 한 쪽 손으로 구슬을 만지고, 또 다른 한 손으로는 장대의 뿌리를 훑었다…
	ELSE
		PRINTL 페니스를 빨면서 장대의 뿌리를 훑었다…
	ENDIF
;--------------------------------------------------------
;바큠펠라
;--------------------------------------------------------
ELSEIF SELECTCOM == 127
	PRINTFORM %타겟은()% %CALLNAME:PLAYER%의 귀두를 혀로 
	IF TALENT:52
		PRINT 빨아돌리며, 
	ELSE
		PRINT 빨아올리며, 
	ENDIF
	PRINT 뺨을 움츠린채로 음란한 소리를 지르면서, 
	SIF ABL:12 >= 4 && ABL:13 >= 3
		PRINT 허리가 빠질지도 모른다고 생각할만큼 
	PRINTL 강하게 빨았다…
;--------------------------------------------------------
;정상위・키스
;--------------------------------------------------------
ELSEIF SELECTCOM == 128
	PRINTFORML %타겟을()% 꿰뚫은채로 겹치듯이 몸을 넘어뜨려, 입술을 겹치고 혀를 섞었다…
;--------------------------------------------------------
;정상위・가슴애무
;--------------------------------------------------------
ELSEIF SELECTCOM == 129
	PRINTFORM %타겟을()% 꿰뚫은채로 
	;거유か폭유
	IF TALENT:110 || TALENT:114
		PRINT 탄력있게 흔들리는 유방을 
	ELSEIF TALENT:109
		PRINT 작은 가슴을 
	ELSE
		PRINT 흔들리는 가슴을 
	ENDIF
	PRINTL 주물렀다…
;--------------------------------------------------------
;정상위SP
;--------------------------------------------------------
ELSEIF SELECTCOM == 130
		PRINTFORML %타겟과()% 겹치듯이 몸을 넘어뜨리고, 입술을 겹치며 혀를 섞어, 
		IF TALENT:110 || TALENT:114
			PRINT 탄력있게 흔들리는 유방을 주무르며 
		ELSEIF TALENT:109
			PRINT 작은 가슴을 만지작거리며 
		ELSE
			PRINT 흔들리는 가슴을 주무르며 
		ENDIF	
		SIF TALENT:85 || TALENT:90
		PRINT 양손의 손가락을 서로 얽히도록 잡고, 
		PRINTW 파내듯이 허리를 흔들며 자궁을 찔렀다…
	IF TFLAG:899 == 0
		IF EXP:0 <= 30 || (EXP:0 >= 31 && EXP:0 <= 50)
			PRINTFORMW %타겟은()% 이물감이 강한것인지, 괴로워하고 있는 것 같다…
		ELSEIF EXP:0 <= 50 && TALENT:121 && TEQUIP:89 == 0
			PRINTFORMW %타겟은()% 음모와 배꼽을 페니스로 희롱당해, 콧소리를 흘리고 있다…
		ELSEIF EXP:0 <= 50 && TEQUIP:89 == 0
			PRINTFORMW %타겟은()% 음모에 클리토리스를 문질러져, 콧소리를 흘리고 있다…
		ELSEIF EXP:0 <= 80
			PRINTFORMW %타겟은()% 길고 달콤한 한숨을 내쉬며, 삽입감에 이를 꽉 물고 있다…
		ELSEIF EXP:0 <= 120
			PRINTFORML %CALLNAME:TARGET%의 자궁이 자식을 받아들일 준비를 마친 것 처럼, 끝 부분의 굴곡이 질 안쪽으로 안내한다…
			PRINTFORMW %CALLNAME:TARGET%의 자궁을 노크할 때마다, 입을 뻐끔뻐끔하며 격렬하게 떨고 있다…
		ELSE
			PRINTFORML 낮은 위치로 내려온 %CALLNAME:TARGET%의 자궁을 찌르며 흔들면, 
			PRINTFORM %타겟은()% 
			SIF TALENT:85
				PRINT 눈망울에 눈물을 띄우고 
			SIF PALAM:5 > PALAMLV:4
				PRINT 침을 흘리고 
			SIF PALAM:3 > PALAMLV:4
				PRINT 새하얀 액체로 질을 채우며 
			PRINTFORMW 미친듯이 괴로워하며, 암컷으로서 최고의 쾌락에 빠졌다…
		ENDIF
	ENDIF
;--------------------------------------------------------
;후배위・가슴애무
;--------------------------------------------------------
ELSEIF SELECTCOM == 131
	PRINTFORM %CALLNAME:TARGET%의 
	IF TALENT:110 || TALENT:114
		PRINT 탄력있게 흔들리는 유방을 뒤에서 덥석 쥐고 주물거리며 
	ELSEIF TALENT:109
		PRINT 작은 가슴을 뒤에서 만지작거리며 
	ELSE
		PRINT 흔들리는 가슴을 뒤에서 주무르며 
	ENDIF
	PRINTL 허리를 쳐올렸다…
;--------------------------------------------------------
;후배위・스팽킹
;--------------------------------------------------------
ELSEIF SELECTCOM == 132
	PRINTFORML %타겟을()% 뒤에서 범하면서, 떨리는 엉덩이를 손으로 때렸다…
	IF TFLAG:899 == 0
		IF ABL:21 < 3
			PRINTFORML %타겟은()% 둔부에 전해지는 고통에 얼굴을 찡그리며 범해지고 있다
		ELSEIF ABL:21 >= 3 && ABL:21 < 5
			PRINTFORML %타겟은()% 얻어맞으면서도 비부가 흠뻑 젖어, 엉덩이를 흔들며 조르듯이 허리를 움직이며 스스로 쾌락을 탐내고 있다…
		ELSE
			PRINTFORML 엉덩이를 새빨갛게 될 정도로 얻어맞아, %타겟은()% 아픔과 질을 파내어지는 쾌락에 비명을 지르며 미친듯이 괴로워했다…
		ENDIF
	ENDIF
;--------------------------------------------------------
;서서삽입
;--------------------------------------------------------
ELSEIF SELECTCOM == 133
	PRINTFORM 삽입하고 있는 %CALLNAME:TARGET%의 상체를 일으켜, 뒤에서부터 
	IF TALENT:110 || TALENT:114
		PRINTL 탄력있게 흔들리는 유방을 뒤에서 덥석 쥐고 주물거리며, 
	ELSEIF TALENT:109
		PRINTL 작은 가슴을 뒤에서 만지작거리며, 
	ELSE
		PRINTL 흔들리는 가슴을 뒤에서 주무르며, 
	ENDIF
	PRINT 턱을 잡고 이 쪽을 뒤돌아보게 하고 
	IF TALENT:85
		PRINT 혀를 섞으며 농후한 키스를 하면서 
	ELSE
		PRINT 혀를 섞으며 입안을 유린하면서 
	ENDIF
	PRINTL 허리를 찔러올렸다…
;--------------------------------------------------------
;후배위SP
;--------------------------------------------------------
ELSEIF SELECTCOM == 134
	PRINTFORM %CALLNAME:TARGET%의 
	IF TALENT:110 || TALENT:114
		PRINTL 탄력있게 흔들리는 유방을 뒤에서 덥석 쥐고 주물거리며, 
	ELSEIF TALENT:109
		PRINTL 작은 가슴을 뒤에서 만지작거리며, 
	ELSE
		PRINTL 흔들리는 가슴을 뒤에서 주무르며, 
	ENDIF
	PRINT 턱을 잡고 이 쪽을 뒤돌아보게 하고 
	IF TALENT:85
		PRINT 혀를 섞으며 농후한 키스를 하면서 
	ELSE
		PRINT 혀를 섞으며 입안을 유린하면서 
	ENDIF
	PRINTFORML %CALLNAME:TARGET%의 양 팔을 잡고 허리를 잡아 당기는 것 같은 자세로 페니스를 찔러올리며 자궁구를 괴롭혔다…
	IF TFLAG:899 == 0
		IF EXP:0 <= 30 || (EXP:0 >= 31 && EXP:0 <= 50)
			PRINTFORMW %타겟은()% 이물감이 강한것인지, 괴로워하고 있는 것 같다…
		ELSEIF EXP:0 <= 50 && TALENT:121
			PRINTFORMW %타겟은()% 음모와 배꼽을 페니스로 희롱당해, 콧소리를 흘리고 있다…
		ELSEIF EXP:0 <= 50
			PRINTFORMW %타겟은()% 음모에 클리토리스를 문질러져, 콧소리를 흘리고 있다…
		ELSEIF EXP:0 <= 80
			PRINTFORMW %타겟은()% 길고 달콤한 한숨을 내쉬며, 삽입감에 이를 꽉 물고 있다…
		ELSEIF EXP:0 <= 120
			PRINTFORML 자궁구가 자식을 받아들일 준비를 마친 것 처럼, 끝 부분의 굴곡이 질 안쪽으로 안내한다…
			PRINTFORMW %CALLNAME:TARGET%의 자궁을 노크할 때마다, 입을 뻐끔뻐끔하며 격렬하게 떨고 있다…
		ELSE
			PRINTFORML 낮은 위치로 내려온 %CALLNAME:TARGET%의 자궁을 찌르며 흔들면, 
			PRINTFORM %타겟은()% 
			SIF TALENT:85
				PRINT 눈망울에 눈물을 띄우고, 
			SIF PALAM:5 > PALAMLV:4
				PRINT 침을 흘리고 
			SIF PALAM:3 > PALAMLV:4
				PRINT 새하얀 액체로 질을 채우며 
			PRINTFORMW 미친듯이 괴로워하며, 암컷으로서 최고의 쾌락에 빠졌다…
		ENDIF
	ENDIF

	;처녀상실の後で페니스を抜いているか
	IF TFLAG:31 == 2
		TFLAG:31 = 1
	ELSE
		TFLAG:31 = 0
	ENDIF
;--------------------------------------------------------
;안면기승 플레이
;--------------------------------------------------------
ELSEIF SELECTCOM == 135
	IF TEQUIP:55
		PRINTFORM %타겟은()% 
			IF ASSI >= 0
				PRINTFORML %CALLNAME:ASSI%의 얼굴 위에서 떨어졌다
			ELSE
				PRINTFORML %CALLNAME:PLAYER%의 얼굴 위에서 떨어졌다
			ENDIF
	ELSE
		PRINTFORM %타겟은()% 
			IF ASSI >= 0
				PRINTFORM %CALLNAME:PLAYER%의 얼굴에 자신의 
				 IF TALENT:TARGET:128 == 1
				 	PRINTL 풍만한 엉덩이를 꽉 눌렀다
				 ELSE
				 	PRINTL 엉덩이를 꽉 눌렀다
				 ENDIF
			ELSE
				PRINTFORM %CALLNAME:PLAYER%의 얼굴에 자신의 
				 IF TALENT:TARGET:128 == 1
				 	PRINTL 풍만한 엉덩이를 꽉 눌렀다
				 ELSE
				 	PRINTL 엉덩이를 꽉 눌렀다
				 ENDIF
			ENDIF
	ENDIF

;--------------------------------------------------------
;칭찬한다
;--------------------------------------------------------
ELSEIF SELECTCOM == 136
		PRINTFORML %플레이어는()% %CALLNAME:TARGET%의 머리를 쓰다듬었다
		SIF TALENT:85 
			PRINTFORML %타겟은()% 눈을 가늘게 뜨고 기뻐하고 있다
;--------------------------------------------------------
;삼각목마
;--------------------------------------------------------
ELSEIF SELECTCOM == 137
	IF TEQUIP:70
		PRINTFORML %타겟을()% 삼각목마에서 내려줬다…
	ELSE
		PRINTFORML %타겟을()% 삼각목마에 태우고 구속했다…
	ENDIF
;;--------------------------------------------------------
;;ボイストレーニング
;;--------------------------------------------------------
;ELSEIF SELECTCOM == 138
;	PRINTFORM 調教部屋に%CALLNAME:TARGET%の
;		;가창기능が0
;		IF ABL:71 == 0
;			PRINT 下手な
;		;가창기능が1
;		ELSEIF ABL:71 == 1
;			PRINT 普通の
;		;가창기능が3以下
;		ELSEIF ABL:71 >= 3
;			PRINT 上手な
;		;가창기능が5以上
;		ELSEIF ABL:71 >= 5
;			PRINT 素晴しい
;		;가창기능が10
;		ELSEIF ABL:71 == 10
;			PRINT 心奪われる
;		ENDIF
;	PRINTL 歌声が響いた…
;;--------------------------------------------------------
;;ダンストレーニング
;;--------------------------------------------------------
;ELSEIF SELECTCOM == 139
;	PRINTFORM %CALLNAME:TARGET%は%CALLNAME:PLAYER%に
;		;무용기능が0
;		IF ABL:72 == 0
;			PRINT 下手な
;		;무용기능が1
;		ELSEIF ABL:72 == 1
;			PRINT 普通の
;		;무용기능が3以下
;		ELSEIF ABL:72 >= 3
;			PRINT 上手な
;		;무용기능が5以上
;		ELSEIF ABL:72 >= 5
;			PRINT 素晴しい
;		;무용기능が10
;		ELSEIF ABL:72 == 10
;			PRINT 目奪われる
;		ENDIF
;	PRINTL ダンスを披露した…
;;--------------------------------------------------------
;;ビジュアルトレーニング
;;--------------------------------------------------------
;;ELSEIF SELECTCOM == 140
;;	PRINTFORM %CALLNAME:TARGET%
;;		IF ABL:70 == 0
;;			PRINT 下手な
;;		ELSEIF ABL:70 == 1
;;			PRINT 普通の
;;		;가창기능が3より下
;;		ELSEIF ABL:70 > 3
;;			PRINT 上手な
;;		ELSEIF ABL:70 >= 5
;;			PRINT 素晴しい
;;		ELSEIF ABL:70 == 10
;;			PRINT 至高の
;;		ENDIF
;;	PRINTL 歌声が響いた…
;ENDIF
						;****슬라임パッチ追加ここから****
;--------------------------------------------------------
;슬라임 생성
;--------------------------------------------------------
ELSEIF SELECTCOM == 150
	IF TEQUIP:150
		PRINTFORML %플레이어가()% 술법을 해제하자 %CALLNAME:TARGET%에게 달라붙어있던 슬라임은 형체도 없이 사라졌다
	ELSE
		IF NO:PLAYER == 104
			PRINTFORML %CALLNAME:PLAYER%의 마력으로 생겨난 슬라임이 %CALLNAME:TARGET%에게 달라붙었다…
		ELSE
			PRINTFORML 푸른 마술서의 마력으로 생겨난 슬라임이 %CALLNAME:TARGET%에게 달라붙었다…
		ENDIF
	ENDIF
;--------------------------------------------------------
;슬라임 질내진입
;--------------------------------------------------------
ELSEIF SELECTCOM == 151
	IF TEQUIP:151
		PRINTFORML %CALLNAME:TARGET%의 질내에 들어갔던 슬라임이 밖으로 나오기 시작했다…
	ELSE
		PRINTFORML 슬라임이 %CALLNAME:TARGET%의 질내로 들어가 자극을 준다…
	ENDIF
;--------------------------------------------------------
;슬라임 항문진입
;--------------------------------------------------------
ELSEIF SELECTCOM == 152
	IF TEQUIP:152
		PRINTFORML %CALLNAME:TARGET%의 항문에 들어갔던 슬라임이 밖으로 나오기 시작했다…
	ELSE
		PRINTFORML 슬라임이 %CALLNAME:TARGET%의 애널로 들어가 자극을 준다…
	ENDIF
;--------------------------------------------------------
;슬라임자위
;--------------------------------------------------------
ELSEIF SELECTCOM == 153
	
	IF TALENT:122
		SAVESTR:2 = 음경을 문지르고
		SAVESTR:3 = 수컷
	ELSEIF TALENT:121 == 0
		SAVESTR:2 = 비부를 만지작거리고
		SAVESTR:3 = 암컷
	ELSEIF TALENT:121 == 1
		SAVESTR:2 = 발기한 후타나리 페니스를 문지르며 비부를 만지작거리고
		SAVESTR:3 = 암컷
	ENDIF
	
	IF A < V
		PRINTFORML %CALLNAME:TARGET%에게 자위를 명령해보았지만 거부당했다. 좀 더 지도할 필요가 있다.
	ELSEIF A > V && A < 50
		PRINTFORM %플레이어가()% 자위를 명령하자, %타겟은()% 
		;着衣設定
		IF CFLAG:40 & 8
			CALL PRINT_CLOTHTYPE_MAIN2
			PRINT 의 앞을 올려 성기를 드러내고 
		ENDIF
		SIF TALENT:15
			PRINT 자존심은 굽히지 않고 
		IF TALENT:22
			PRINT 무표정한 얼굴로 묵묵히 
		ELSEIF TALENT:10 || TALENT:14
			PRINT 떨리는 손 끝으로 
		ELSEIF TALENT:11
			PRINT 반항적으로 노려봤지만 
		ELSEIF TALENT:12
			PRINT 굴욕으로 입술을 깨물고 
		ELSEIF TALENT:35
			PRINT 수치심에 귀까지 완전히 빨개져 
		ENDIF
		SIF TEQUIP:53
			PRINT 비디오카메라 앞에서 
		PRINTL 자위를 시작했다.
	ELSEIF A >=50 && A < 80
		PRINTFORML 지도의 성과인지, %타겟은()% %CALLNAME:PLAYER%의 명령에 복종해 자위를 시작했다.
	ELSEIF A >= 80
		PRINTFORML %플레이어가()% 명령하자 기뻐하며, %타겟은()% 완전히 사육된 %SAVESTR:3%의 표정으로 자위를 시작했다.
	ENDIF
	IF TEQUIP:151 && TEQUIP:152 && ABL:2 >= 5 && ABL:3 >= 5 && ABL:0 >= 5 && ABL:1 >= 5 && PALAM:5 >= PALAMLV:4 && PREVCOM == 3
		PRINTFORML %타겟은()% 침을 흘리며, 움찔움찔 몸을 떨면서 울고 있다
		PRINTFORML 절정이 멈추지 않아 계속해서 가고 있는 것 같다…
	ELSEIF TEQUIP:151 && ABL:2 >= 5 && ABL:0 >= 5 && ABL:1 >= 5 && PALAM:5 >= PALAMLV:4 && PREVCOM == 3
		PRINTFORML %타겟은()% 애액이 흩날리는 것도 신경쓰지 않고, 슬라임이 벌리고 있는 질내를 손가락으로 해집고 있다
		PRINTFORML 이제는 다른 일을 신경쓸 여유도 없는 것 같다…
	ELSEIF TEQUIP:152 && ABL:3 >= 5 && ABL:0 >= 5 && ABL:1 >= 5 && PALAM:5 >= PALAMLV:4 && PREVCOM == 3
		PRINTFORML %타겟은()% 부들부들 몸을 떨면서, 슬라임이 벌리고 있는 애널을 손가락으로 해집고 있다
		PRINTFORML 주름을 밀고 들어올때마다 몇번이나 절정에 도달해버리는 것 같다…
	ELSEIF ABL:10 >= 3 && ABL:0 >= 5 && ABL:1 >= 5 && PALAM:5 >= PALAMLV:4 && PREVCOM == 153
		PRINTFORML %타겟은()% 교성을 흘리며 달라붙는 슬라임은 신경도 안쓰고 일심분란하게 자신의 %SAVESTR:2% 있다
		PRINTFORML 멈추고 싶어도 손이 멋대로 움직여 멈춰지지 않는 것 같다…
	ENDIF

	SAVESTR:2 = %STR:0%
	SAVESTR:3 = %STR:0%
;--------------------------------------------------------
;슬라임 구욕
;--------------------------------------------------------
ELSEIF SELECTCOM == 154
	IF TEQUIP:154
		PRINTFORML 슬라임은 %CALLNAME:TARGET%의 입에서 빠져나왔다…
	ELSE
		PRINTFORM 슬라임은 
		IF CFLAG:42 == 11 && (CFLAG:40 & 64)
			CALL PRINT_CLOTHTYPE_SPECIAL
			PRINTFORM 의 틈새를 파고들어 
		ENDIF
		PRINTFORML %CALLNAME:TARGET%의 입으로 들어갔다…
	ENDIF
;--------------------------------------------------------
;수음펠라
;--------------------------------------------------------
ELSEIF SELECTCOM == 138
	PRINTFORM %타겟은()% 
	SIF TALENT:88
		PRINT 황홀한 표정으로 
	PRINTFORM 엎드려있는 %CALLNAME:PLAYER%의 페니스를 문지르며, %CALLNAME:PLAYER%의 
	IF ABL:PLAYER:3 >= 3
		PRINTFORM 쾌락으로 벌렁이는 
	ELSE
		PRINTFORM 단단히 닫힌 
	ENDIF
	PRINTL 연분홍색 구멍 안으로 혀를 넣었다…
	IF (CFLAG:7 & 16)
		PRINTFORM %CALLNAME:TARGET%의 혀끝에 달린 피어스가 %CALLNAME:PLAYER%의 
		IF TALENT:PLAYER:122 == 1
			PRINTFORM 전립선
		ELSE
			PRINTFORM 장벽
		ENDIF
		PRINTFORML 에 닿을때마다 %플레이어는()% 신음소리를 냈다…
	ENDIF
;--------------------------------------------------------
;구현
;--------------------------------------------------------
ELSEIF SELECTCOM == 180
	IF TALENT:121 == 0
		PRINTFORM %CALLNAME:PLAYER%에게 명령받아 %타겟은()% 
		PRINTFORM 자신의 음핵을 %CALLNAME:TARGET% 같은 
		SIF MAXBASE:31 >= 120
			PRINTFORM 미
		IF BASE:9 <= 19
			PRINTFORM 소녀
		ELSE
			PRINTFORM 녀
		ENDIF
		PRINTFORML 에게는 어울리지 않는, 사정기능이 달린 육봉으로 구현화했다…
		IF PALAM:0 > PALAMLV:3
			PRINTFORML 클리토리스와 감각이 연결돼있는 구현화 페니스는 이미 혈관이 보일만큼 커져서, 
			PRINTFORML %CALLNAME:TARGET%의 열기를 보여주듯 귀두에선 카우퍼액을 흘리고 있었다
		ENDIF
	ELSE
		PRINTFORM %CALLNAME:PLAYER%에게 명령받아 %타겟은()% 
		PRINTFORML 구현화시킨 페니스를 소거했다…
		IF ABL:38 >= 3
			PRINTFORM 가짜라도 사정의 쾌감을 기억해버린
			PRINTFORML %타겟은()% 무의식적으로 페니스가 있었던 부분을 문지르고 있다
		ENDIF
	ENDIF
ENDIF

						;****슬라임パッチ追加ここまで****
RETURN 1
