;-------------------------------------------------
;クリアボーナス: BF
;-------------------------------------------------
@CLEARBONUS_BOYFRIEND

IF  TALENT:122 == 0 && TALENT:206 == 0 && TALENT:184 == 0
	PRINTFORML %타겟이(1)% 남친을 가진 IF세계로 갈까요?
	PRINTL  [0] - 네
	PRINTL  [1] - 아니오
	$INPUT_LOOP_5
	INPUT
		IF RESULT == 0
			CALL DEFOLT_GENITALCALL
			CALL CLEARBONUS_BOYFRIEND_ADDEXP
		ELSEIF RESULT == 1
			CALL CALC_BEAUTY
		ELSE
			GOTO INPUT_LOOP_5
		ENDIF
ENDIF

;-------------------------------------------------
;クリアボーナス: BF　処理
;-------------------------------------------------
@CLEARBONUS_BOYFRIEND_ADDEXP

REPEAT 30
	LOCAL:COUNT = 0
REND

$INPUT_LOOP_000
LOCAL = TARGET
CFLAG:LOCAL:621 = 1

IF FLAG:541 == 1
	CFLAG:LOCAL:622 = 0
ELSE
	PRINTFORML %CALLNAME:LOCAL%의 남친은……
	PRINTL  [0] - 양키
	PRINTL  [1] - 반 친구
	PRINTL  [2] - 고등학생
	PRINTL  [3] - 중학생
	PRINTL  [4] - 대학생
	PRINTL  [5] - 회사원
	PRINTL  [6] - 호스트
	$INPUT_LOOP_00
	INPUT
	IF RESULT >= 0 && RESULT <= 6
		CFLAG:LOCAL:622 = RESULT
	ELSE
		GOTO INPUT_LOOP_00
	ENDIF
ENDIF  	

LOCAL:5 = 0
IF CFLAG:LOCAL:622 == 0
	LOCAL:5 = RAND:100
		SIF NO:LOCAL == 1
			LOCAL:5 = 100
		SIF NO:LOCAL == 2
			LOCAL:5 = 100
		IF LOCAL:5 >= 85
			TALENT:LOCAL:432 = 1
			LOCAL:6 = 1
			CSTR:LOCAL:80 = 자지
			CSTR:LOCAL:81 = 보지
			CSTR:LOCAL:82 = 애널
			IF RAND:2 == 0
				CSTR:LOCAL:9 = 나
			ELSE
				CSTR:LOCAL:9 = 난
			ENDIF
			SIF NO:LOCAL == 1
				CSTR:LOCAL:9 = 난
		ENDIF
ENDIF

LOCAL:7 = 0
LOCAL:7 = RAND:100
	IF LOCAL:7 >= 85
		TALENT:LOCAL:423 = 1
		LOCAL:8 = 1
	ENDIF

TALENT:LOCAL:184 = 1

CALL DECIDE_BOYFRIEND,LOCAL

IF CFLAG:LOCAL:16 == -1
	PRINTFORML %CALLNAME:LOCAL%의 첫 키스 상대는……
	PRINTL  [0] - 남친
	PRINTL  [1] - 아직
	$INPUT_LOOP_01
	INPUT
	IF RESULT == 1
		PRINT
	ELSEIF RESULT == 0
		CFLAG:LOCAL:16 = RAND:3
		SIF CFLAG:LOCAL:16 == 0
			CFLAG:LOCAL:16 = 2
		IF CFLAG:LOCAL:16 == 1
			CSTR:LOCAL:1 = %CSTR:LOCAL:7% %CSTR:LOCAL:8%(남친)
			BASE:LOCAL:31 -= 5
		ELSEIF CFLAG:LOCAL:16 == 2
			CFLAG:LOCAL:16 = 3
			IF TALENT:432 == 0
				CSTR:LOCAL:1 = %CSTR:LOCAL:7% %CSTR:LOCAL:8%
				CFLAG:824 = 1
			ELSE
				CSTR:LOCAL:1 = %CSTR:LOCAL:7% %CSTR:LOCAL:8%
				CFLAG:824 = 1
			ENDIF
			BASE:LOCAL:31 -= 10
		ENDIF
	ELSE
		GOTO INPUT_LOOP_01
	ENDIF	
ENDIF

IF CFLAG:LOCAL:15 == 0 && TALENT:LOCAL:0 == 1
	PRINTFORML %CALLNAME:LOCAL%의 처녀 상실 상대는……
	PRINTL  [0] - 남친
	PRINTL  [1] - 아직 처녀입니다
	$INPUT_LOOP_02
	INPUT
	IF RESULT == 1
		PRINT
	ELSEIF RESULT == 0
		CSTR:LOCAL:0 = %CSTR:LOCAL:7% %CSTR:LOCAL:8%(남친)
		CFLAG:LOCAL:15 = 20
		TALENT:LOCAL:0 = 0
		BASE:31 -= 10
	ELSE
		GOTO INPUT_LOOP_02
	ENDIF		
ENDIF

IF TALENT:LOCAL:0 == 0
	PRINTFORML %조사처리(CALLNAME:LOCAL,"는")%……
	PRINTL  [0] - 남친과 마구 해대서 경험 풍부함
	PRINTL  [1] - SEX는 익숙해짐
	PRINTL  [2] - 얼마 전 처녀상실
	$INPUT_LOOP_03
	INPUT
	IF RESULT == 0
		TCVAR:LOCAL:0 += 40 + RAND:31
		TCVAR:LOCAL:5 += 30 + RAND:31
		TCVAR:LOCAL:20 += TCVAR:LOCAL:5 - RAND:11
		TCVAR:LOCAL:2 += TCVAR:LOCAL:5 - RAND:11
		ABL:LOCAL:2 += 3 + RAND:3
		ABL:LOCAL:11 += 2
		ABL:LOCAL:14 += 2
		ABL:LOCAL:30 += 1
		CFLAG:160 = (BASE:9 - RAND:4)
		SIF CFLAG:160 == BASE:9
			CFLAG:160 = BASE:9 - 1
		CFLAG:161 = (DAY:1 - RAND:4)
		SIF CFLAG:161 <= 0
			CFLAG:161 = 12
		IF CFLAG:160 >= BASE:9
			 SIF CFLAG:161 <= BASE:11
				CFLAG:160 -= 1
		ENDIF	
		SIF CFLAG:161 == DAY:1 && CFLAG:162 >= DAY:2
			CFLAG:161 = DAY:2
		CFLAG:162 = RAND:4
		SIF CFLAG:162 == 0
			CFLAG:162 = 1
		CFLAG:163 = 3
		CFLAG:164 = 3
	ELSEIF RESULT == 1
		TCVAR:LOCAL:0 += 20 + RAND:11
		TCVAR:LOCAL:5 += 15 + RAND:11
		TCVAR:LOCAL:20 += TCVAR:LOCAL:5 - RAND:6
		TCVAR:LOCAL:2 += TCVAR:LOCAL:5 - RAND:6
		ABL:LOCAL:2 += 1 + RAND:3
		ABL:LOCAL:11 += 1
		ABL:LOCAL:14 += 1
		CFLAG:160 = BASE:9
		CFLAG:161 = (DAY:1 - RAND:4)
		SIF CFLAG:161 <= 0
			CFLAG:161 = 12
		CFLAG:162 = RAND:4
		SIF CFLAG:161 == DAY:1 && CFLAG:162 >= DAY:2
			CFLAG:161 = DAY:2
		CFLAG:163 = 3
		CFLAG:164 = 0
	ELSEIF RESULT == 2
		TCVAR:LOCAL:0 += 1 + RAND:5
		TCVAR:LOCAL:5 += TCVAR:LOCAL:0 - RAND:3
		SIF TCVAR:LOCAL:5 <= 0
			TCVAR:LOCAL:5 = 1
		TCVAR:LOCAL:20 += TCVAR:LOCAL:5 - RAND:2
		SIF TCVAR:LOCAL:5 <= 0
			TCVAR:LOCAL:20 = 0
		TCVAR:LOCAL:2 += RAND:2
		ABL:LOCAL:2 +=  RAND:2
		CFLAG:160 = BASE:9
		CFLAG:161 = (DAY:1 - RAND:2)
		SIF CFLAG:161 <= 0
			CFLAG:161 = 12
		CFLAG:162 = RAND:4
		SIF CFLAG:161 == DAY:1 && CFLAG:162 >= DAY:2
			CFLAG:161 = DAY:2
		CFLAG:163 = 3
		CFLAG:164 = 0
	ELSE
		GOTO INPUT_LOOP_03
	ENDIF		
ENDIF

IF TALENT:LOCAL:2 == 1
	PRINTFORML %조사처리(CALLNAME:LOCAL,"는")%……
	IF CFLAG:LOCAL:15 == 20
		PRINTL  [0] - 애널처녀도 남친에게 바쳤습니다
	ELSE
		PRINTL  [0] - 애널처녀는 남친에게 바쳤습니다
	ENDIF

	IF TALENT:LOCAL:0 == 0
		PRINTL  [1] - 애널은 아직 처녀입니다
	ELSE
		PRINTL  [1] - 애널도 처녀입니다
	ENDIF
	$INPUT_LOOP_04
	INPUT

	IF RESULT == 1
		PRINT
	ELSEIF RESULT == 0
		CFLAG:LOCAL:616 = 8
		CSTR:LOCAL:3 = %CSTR:LOCAL:7% %CSTR:LOCAL:8%(남친)
		TALENT:LOCAL:2 = 0
		BASE:31 -= 5
	ELSE
		GOTO INPUT_LOOP_04
	ENDIF		
ENDIF

IF TALENT:LOCAL:2 == 0
	PRINTFORML %조사처리(CALLNAME:LOCAL,"는")%……
	IF CFLAG:LOCAL:15 == 20
		PRINTL  [0] - 남친과 애널로도 마구 해대서 경험 풍부함
	ELSE
		PRINTL  [0] - 남친과 애널로 마구 해대서 경험 풍부함
	ENDIF
	IF CFLAG:LOCAL:15 == 20
		PRINTL  [1] - 애널SEX도 익숙해짐
	ELSE
		PRINTL  [1] - 애널SEX는 이제 익숙해짐
	ENDIF
	IF CFLAG:LOCAL:15 == 20
		PRINTL  [2] - 얼마 전 애널처녀도 상실
	ELSE
		PRINTL  [2] - 얼마 전 애널처녀를 상실
	ENDIF
	$INPUT_LOOP_05
	INPUT
	IF RESULT == 0
		TCVAR:LOCAL:1 += 40 + RAND:31
		TCVAR:LOCAL:5 += 30 + RAND:31
		TCVAR:LOCAL:20 += TCVAR:LOCAL:5 - RAND:11
		ABL:LOCAL:3 += 3 + RAND:3
		TCVAR:LOCAL:2 += TCVAR:LOCAL:5 - RAND:11
		SIF ABL:LOCAL:3 >= 5
			ABL:LOCAL:3 = 5
		ABL:LOCAL:11 += 2
		ABL:LOCAL:14 += 2
		ABL:LOCAL:30 += 1
		CFLAG:LOCAL:830 = (BASE:9 - RAND:4)
		SIF CFLAG:LOCAL:830 == BASE:9
			CFLAG:LOCAL:830 = BASE:9 - 1
		CFLAG:LOCAL:831 = (DAY:1 - RAND:4)
		SIF CFLAG:LOCAL:831 <= 0
			CFLAG:LOCAL:831 = 12
		IF CFLAG:LOCAL:830 >= BASE:9
			 SIF CFLAG:LOCAL:831 <= BASE:11
				CFLAG:LOCAL:830 -= 1
		ENDIF	
		SIF CFLAG:LOCAL:831 == DAY:1 && CFLAG:LOCAL:832 >= DAY:2
			CFLAG:LOCAL:831 = DAY:2
		CFLAG:LOCAL:832 = RAND:4
		SIF CFLAG:LOCAL:832 == 0
			CFLAG:LOCAL:832 = 1
		CFLAG:LOCAL:833 = 3
		CFLAG:LOCAL:834 = 3
	ELSEIF RESULT == 1
		TCVAR:LOCAL:1 += 20 + RAND:11
		TCVAR:LOCAL:5 += 15 + RAND:11
		TCVAR:LOCAL:20 += TCVAR:LOCAL:5 - RAND:6
		TCVAR:LOCAL:2 += TCVAR:LOCAL:5 - RAND:6
		ABL:LOCAL:3 += 1 + RAND:3
		SIF ABL:LOCAL:3 >= 3
			ABL:LOCAL:3 = 3
		ABL:LOCAL:11 += 1
		ABL:LOCAL:14 += 1
		CFLAG:830 = BASE:9
		CFLAG:LOCAL:830 = BASE:9
		CFLAG:LOCAL:831 = (DAY:1 - RAND:4)
		SIF CFLAG:LOCAL:831 <= 0
			CFLAG:LOCAL:831 = 12
		CFLAG:LOCAL:832 = RAND:4
		SIF CFLAG:LOCAL:831 == DAY:1 && CFLAG:LOCAL:832 >= DAY:2
			CFLAG:LOCAL:831 = DAY:2
		CFLAG:LOCAL:833 = 3
		CFLAG:LOCAL:834 = 0
	ELSEIF RESULT == 2
		TCVAR:LOCAL:1 += 1 + RAND:5
		TCVAR:LOCAL:5 += TCVAR:LOCAL:0 - RAND:3
		SIF TCVAR:LOCAL:5 <= 0
			TCVAR:LOCAL:5 = 1
		TCVAR:LOCAL:20 += TCVAR:LOCAL:5 - RAND:2
		SIF TCVAR:LOCAL:20 <= 0
			TCVAR:LOCAL:20 = 0
		ABL:LOCAL:3 += 3 + RAND:3
		SIF ABL:LOCAL:3 >= 3
			ABL:LOCAL:3 = 3
		TCVAR:LOCAL:2 += RAND:2
		CFLAG:LOCAL:830 = BASE:9
		CFLAG:LOCAL:831 = (DAY:1 - RAND:2)
		SIF CFLAG:LOCAL:831 <= 0
			CFLAG:LOCAL:831 = 12
		CFLAG:LOCAL:832 = RAND:4
		SIF CFLAG:LOCAL:831 == DAY:1 && CFLAG:LOCAL:832 >= DAY:2
			CFLAG:LOCAL:831 = DAY:2
		CFLAG:LOCAL:833 = 3
		CFLAG:LOCAL:834 = 0
	ELSE
		GOTO INPUT_LOOP_05
	ENDIF		
ENDIF

CFLAG:LOCAL:631 = RAND:100
SIF CFLAG:LOCAL:631 >= 75
	CSTR:LOCAL:80 = 자지
	CSTR:LOCAL:81 = 보지
	CSTR:LOCAL:82 = 애널
	TALENT:LOCAL:76 = 1
CFLAG:LOCAL:631 = 0

EXP:LOCAL:0 += TCVAR:LOCAL:0
EXP:LOCAL:1 += TCVAR:LOCAL:1
EXP:LOCAL:2 += TCVAR:LOCAL:2
EXP:LOCAL:5 += TCVAR:LOCAL:5
EXP:LOCAL:20 += TCVAR:LOCAL:20
EXP:LOCAL:101 += TCVAR:LOCAL:5 / 4


SIF CFLAG:LOCAL:15 == 20
	CFLAG:LOCAL:15 = 8
SIF CFLAG:LOCAL:16
	CFLAG:LOCAL:16 = 8

SIF ABL:LOCAL:11 >= 5
	ABL:LOCAL:11 = 5

IF TALENT:LOCAL:76 == 1
	REPEAT 110
		EXP:LOCAL:COUNT *= 2
	REND

	REPEAT 50
		IF COUNT == 10
			CONTINUE
		ELSEIF COUNT == 12
			CONTINUE
		ELSEIF COUNT == 13
			CONTINUE
		ELSEIF COUNT == 15
			CONTINUE
		ELSEIF COUNT == 16
			CONTINUE
		ELSEIF COUNT == 20
			CONTINUE
		ELSEIF COUNT == 21
			CONTINUE
		ELSEIF COUNT == 22
			CONTINUE
		ELSEIF COUNT == 33
			CONTINUE
		ENDIF
		ABL:LOCAL:COUNT *= 2
		SIF ABL:LOCAL:COUNT >= 10
			ABL:LOCAL:COUNT = 10
	REND

	IF ABL:LOCAL:0 >= 6
		TALENT:LOCAL:74 = 1
		TALENT:LOCAL:101 = 0
	ENDIF

	IF ABL:LOCAL:1 >= 6
		TALENT:LOCAL:78 = 1
		TALENT:LOCAL:107 = 0
	ENDIF

	IF ABL:LOCAL:2 >= 6
		TALENT:LOCAL:75 = 1
		TALENT:LOCAL:103 = 0
	ENDIF

	IF ABL:LOCAL:3 >= 6
		TALENT:LOCAL:77 = 1
		TALENT:LOCAL:105 = 0
	ENDIF
ENDIF

IF ABL:LOCAL:11 >= 4
	TALENT:LOCAL:30 = 0
	TALENT:LOCAL:71 = 0
	IF TALENT:LOCAL:82 == 1
		TALENT:LOCAL:82 = 0
		TALENT:LOCAL:81 = 1
	ENDIF
	EXP:LOCAL:50 += RAND:3
	EXP:LOCAL:50 += RAND:5
ENDIF


IF EXP:LOCAL:0 >= 1 || EXP:LOCAL:1 >= 1 || EXP:LOCAL:2 >= 1 || EXP:LOCAL:5 >= 1 || EXP:LOCAL:20 >= 1 || EXP:LOCAL:50 >= 1
	PRINTFORML %CALLNAME:LOCAL%의 계약시점 경험이 
	SIF EXP:LOCAL:0 >= 1
		PRINTFORML %EXPNAME:0%: {EXP:LOCAL:0}
	SIF EXP:LOCAL:1 >= 1
		PRINTFORML %EXPNAME:1%: {EXP:LOCAL:1}
	SIF EXP:LOCAL:2 >= 1
		PRINTFORML %EXPNAME:2%: {EXP:LOCAL:2}
	SIF EXP:LOCAL:5 >= 1
		PRINTFORML %EXPNAME:5%: {EXP:LOCAL:5}
	SIF EXP:LOCAL:20 >= 1
		PRINTFORML %EXPNAME:20%: {EXP:LOCAL:20}
	SIF EXP:LOCAL:50 >= 1
		PRINTFORML %EXPNAME:50%: {EXP:LOCAL:50}
	PRINTFORM 이렇게 변경됐고 
	LOCAL:1 += 1
ENDIF

IF ABL:LOCAL:0 >= 1 || ABL:LOCAL:1 >= 1 || ABL:LOCAL:2 >= 1 || ABL:LOCAL:3 >= 1 || ABL:LOCAL:11 >= 1 || ABL:LOCAL:30 >= 1
	PRINTFORML %CALLNAME:LOCAL%의 각 능력이
	SIF ABL:LOCAL:0 >= 1
		PRINTFORML %ABLNAME:0%: LV{ABL:LOCAL:0}
	SIF ABL:LOCAL:1 >= 1
		PRINTFORML %ABLNAME:1%: LV{ABL:LOCAL:1}
	SIF ABL:LOCAL:2 >= 1
		PRINTFORML %ABLNAME:2%: LV{ABL:LOCAL:2}
	SIF ABL:LOCAL:3 >= 1
		PRINTFORML %ABLNAME:3%: LV{ABL:LOCAL:3}
	SIF ABL:LOCAL:11 >= 1
		PRINTFORML %ABLNAME:11%: LV{ABL:LOCAL:11}
	SIF ABL:LOCAL:30 >= 1
		PRINTFORML %ABLNAME:30%: LV{ABL:LOCAL:30}
	PRINTFORM 이렇게 변경됐고
	LOCAL:1 += 1
ENDIF

IF TALENT:LOCAL:76 == 1 || TALENT:LOCAL:89 == 1
	IF TALENT:LOCAL:76 == 1
		SETCOLOR 0xF58F98
		PRINTFORM 【%TALENTNAME:76%】
		LOCALS:1 = %조사만처리(TALENTNAME:76,"를")%
		RESETCOLOR
	ENDIF
	IF TALENT:LOCAL:89 == 1
		SETCOLOR 0xF58F98
		PRINTFORM 【%TALENTNAME:89%】
		LOCALS:1 = %조사만처리(TALENTNAME:89,"를")%
		RESETCOLOR
	ENDIF
	IF LOCAL:6 == 1
		SETCOLOR 0xF58F98
		PRINTFORM 【%TALENTNAME:432%】
		LOCALS:1 = %조사만처리(TALENTNAME:432,"를")%
		RESETCOLOR
	ENDIF
	IF LOCAL:7 == 1
		SETCOLOR 0xF58F98
		PRINTFORM 【%TALENTNAME:423%】
		LOCALS:1 = %조사만처리(TALENTNAME:423,"를")%
		RESETCOLOR
	ENDIF
	PRINTFORM %LOCALS:1% 가진 상태로 시작
	LOCAL:1 += 1
ENDIF
IF LOCAL:1 >= 1
	PRINTFORML 되며
ENDIF
PRINTFORM 신변조사에 의하면 %CALLNAME:LOCAL%의 남친은 %CSTR:LOCAL:7% %조사처리(CSTR:LOCAL:8,"라")%는 이름의 
IF CFLAG:LOCAL == 0
	PRINTFORM 양키
ELSEIF CFLAG:LOCAL == 1
	PRINTFORM 반 친구
ELSEIF CFLAG:LOCAL == 2
	PRINTFORM 고등학생이
ELSEIF CFLAG:LOCAL == 3
	PRINTFORM 중학생이
ELSEIF CFLAG:LOCAL == 4
	PRINTFORM 대학생아
ELSEIF CFLAG:LOCAL == 5
	PRINTFORM 회사원아
ELSEIF CFLAG:LOCAL == 6
	PRINTFORM 호스트
ENDIF
PRINTFORML 라고 한다……

PRINTFORML 《이대로 시작합니까?》
PRINTL [0] - 네
PRINTL [1] - 아니오
$INPUT_LOOP_DECIDE
INPUT

IF RESULT == 0
	PRINTFORM
ELSEIF RESULT == 1
	LOCAL:2 = NO:LOCAL
	SIF TALENT:LOCAL:76 == 1
		LOCAL:3 = 1
	SIF TALENT:LOCAL:89 == 1
		LOCAL:4 = 1
	CALL キャラ削除, LOCAL
	ADDCHARA LOCAL:2
	GETCHARA (LOCAL:2)
	LOCAL = GETCHARA (LOCAL:2)
	IF LOCAL:3 == 1
		TALENT:LOCAL:76 = 1
			CSTR:LOCAL:80 = 자지
			CSTR:LOCAL:81 = 보지
			CSTR:LOCAL:82 = 애널
	ENDIF
	SIF LOCAL:4 == 1
		TALENT:LOCAL:89 = 1
	TARGET = LOCAL
	CALL WEARING_CLOTH_ALL
	CALL SETTING_UNDERHAIR
	LOCAL = TARGET
	GOTO INPUT_LOOP_000
ELSEIF RESULT == 8826
	PRINTFORML 《디버그용입니다》
	ABL:LOCAL:2 = 10
	ABL:LOCAL:3 = 10
	ABL:LOCAL:11 = 10
	ABL:LOCAL:14 = 10
	ABL:LOCAL:30 = 10
	ABL:LOCAL:31 = 10
	EXP:LOCAL:50 = 5 + RAND:4
	CALL DECIDE_BOYFRIEND,LOCAL	
	PRINTFORML 《남친의 이름을 다시 생성합니다》
	PRINTFORML 남친의 이름: %CSTR:LOCAL:7% %CSTR:LOCAL:8%
	CSTR:LOCAL:80 = 자지
	CSTR:LOCAL:81 = 생삽입SEX 좋아하는 음란 촉촉보지
	CSTR:LOCAL:0 = %CSTR:LOCAL:7% %CSTR:LOCAL:8%
	CSTR:LOCAL:1 = %CSTR:LOCAL:7% %CSTR:LOCAL:8%
	CSTR:LOCAL:3 = %CSTR:LOCAL:7% %CSTR:LOCAL:8%
	CFLAG:LOCAL:824 = 1
	GOTO INPUT_LOOP_DECIDE
ELSE
	GOTO INPUT_LOOP_DECIDE
ENDIF

IF CFLAG:LOCAL:16
	IF CFLAG:LOCAL:160 >= CFLAG:LOCAL:830 && CFLAG:LOCAL:161 >= CFLAG:LOCAL:831
		CFLAG:LOCAL:820 = CFLAG:LOCAL:830
		CFLAG:LOCAL:821 = CFLAG:LOCAL:831
		CFLAG:LOCAL:822 = CFLAG:LOCAL:832
		CFLAG:LOCAL:823 = CFLAG:LOCAL:833
	ELSEIF CFLAG:LOCAL:160 <= CFLAG:LOCAL:830  && CFLAG:LOCAL:161 <= CFLAG:LOCAL:831
		CFLAG:LOCAL:820 = CFLAG:LOCAL:160
		CFLAG:LOCAL:821 = CFLAG:LOCAL:161
		CFLAG:LOCAL:822 = CFLAG:LOCAL:162
		CFLAG:LOCAL:823 = CFLAG:LOCAL:163
	ELSE
		CFLAG:LOCAL:820 = BASE:9
		CFLAG:LOCAL:821 = (DAY:1 - RAND:2)
		SIF CFLAG:LOCAL:821 <= 0
			CFLAG:LOCAL:821 = 12
		CFLAG:LOCAL:822 = RAND:4
		SIF CFLAG:LOCAL:821 == DAY:1 && CFLAG:LOCAL:822 >= DAY:2
			CFLAG:LOCAL:821 = DAY:2
		CFLAG:LOCAL:823 = 3
	ENDIF
ENDIF

;-------------------------------------------------
;EXキャラの紹介・まゆ
;-------------------------------------------------
@SETTING_UNDERHAIR
IF FLAG:36
	;카나데は毛深いのがコンプレックスです
	IF NO:TARGET == 1
		CFLAG:6 = 14
	;이쿠미はひどいことになっています
	ELSEIF NO:TARGET == 11
		CFLAG:6 = 18
	;外人さんは下の毛の処理は常識です
	ELSEIF NO:TARGET == 9
		CFLAG:6 = 1
	ELSEIF NO:TARGET == 18
		CFLAG:6 = 1
	;미노리は下の毛の発育が悪いのがコンプレックスです
	ELSEIF NO:TARGET == 2
		CFLAG:6 = 7
	;음모 없음なら生えない
	ELSEIF TALENT:125
		CFLAG:6 = -1
	;発育のいい키쿄우以外の15歳以下ならまだ毛が薄い
	ELSEIF BASE:9 <= 15 && NO:TARGET != 5
		CFLAG:6 = 6 
	;それ以外なら12に設定
	ELSE
		CFLAG:6 = 12
	ENDIF
ENDIF